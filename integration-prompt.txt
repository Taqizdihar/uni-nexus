MASTER PROMPT — UNI-NEXUS GLOBAL INTEGRATION CENTER
MODUL GLOBAL "INTEGRASI"

FINAL GLOBAL MODULE COMPLETION

SECURE CONNECTION CONTROL PLANE +
PROVIDER REGISTRY +
ENCRYPTED CREDENTIAL VAULT +
GOOGLE WORKSPACE CONNECTORS +
WHATSAPP CLOUD CONNECTOR +
CRAFT MARKETPLACE COMPATIBILITY +
PLANNED EXTERNAL PROVIDERS +
CONNECTION TESTING +
CAPABILITY MANAGEMENT +
SYNC EXECUTION +
SYNC HISTORY +
DIAGNOSTICS +
RBAC +
ORGANIZATION ISOLATION +
BUSINESS UNIT ISOLATION +
AUDIT +
SECRET REDACTION +
SSRF / NETWORK SAFETY +
IDEMPOTENCY +
RESPONSIVE UI +
SMOKE TESTS +
BROWSER ACCEPTANCE +
REGRESSION +
FINAL DEVELOPMENT RESTART


Repository:
Taqizdihar/uni-nexus

Environment:
Claude Code running directly inside the local UNI-NEXUS VS Code workspace.

Model:
Claude Sonnet 5
Effort:
MAX


======================================================================
IMPORTANT CONTEXT
======================================================================

This is the LAST remaining Global UNI-NEXUS module before the owner begins
a separate full-system testing/UAT phase.

The purpose of this task is therefore:

BUILD THE GLOBAL INTEGRATION MODULE COMPLETELY,

but DO NOT unnecessarily redesign unrelated working modules.

Leave UNI-NEXUS in a stable state ready for later full-system testing.


======================================================================
LATEST KNOWN REPOSITORY STATE
======================================================================

The latest main HEAD observed when this prompt was written was:

323323f4dec75fed8c7b56aac93d730eaf23a4e1

Do NOT reset to this SHA.

The user may have pushed newer work before Claude Code starts.

Always inspect the ACTUAL current workspace and origin/main.


======================================================================
BRANCH / GIT RULE
======================================================================

Work directly on:

main

Do NOT create:

- feature branch
- temporary branch
- Claude branch
- worktree branch
- PR branch

At preflight run:

git fetch origin
git branch --show-current
git status
git log -1 --oneline

Branch must remain:

main

If appropriate:

git pull --ff-only origin main

Never:

git reset --hard unrelated user work
git clean away user files
force push
rewrite history

Final report must explicitly say:

BRANCH CREATED: NO
WORKED ONLY ON main: YES


======================================================================
MISSION
======================================================================

Fully implement:

GLOBAL INTEGRATION CENTER
"Pusat Integrasi" / "Integrasi"

at:

/app/integrations

This is NOT a mockup task.

Do not stop after:

- creating cards
- creating a provider list
- making static "Connected" badges
- creating fake OAuth buttons
- creating placeholder sync buttons
- wrapping existing Craft Marketplace APIs
- implementing frontend without backend
- creating backend without security
- creating an architecture document only

Inspect.
Design.
Implement.
Integrate.
Test.
Repair.
Restart.
Verify.


======================================================================
PRODUCT INTENT
======================================================================

Read:

UNI-NEXUS All Modules Description Generated by ChatGPT.md

Preserve its Global Integrations intent:

UNI-NEXUS should be capable of connecting with external services such as:

Google Drive
Google Sheets
Google Calendar
WhatsApp
marketplaces
payment providers
approved APIs / external services

The Integration Center is the central place to understand:

what is connected,
to which workspace,
using which provider,
what it is allowed to do,
whether the connection is healthy,
when it last ran,
what failed,
and what actions are currently supported.


======================================================================
ARCHITECTURAL RULE #1
======================================================================

DO NOT CREATE A SECOND INTEGRATIONS DATA MODEL.

The canonical tables already exist:

integrations
integration_sync_logs

Reuse them.

Do NOT create:

global_integrations
integration_connections
provider_connections
external_integrations
global_sync_logs

as parallel replacements.


======================================================================
ARCHITECTURAL RULE #2
======================================================================

The existing:

server/src/shared/integrations/

area must evolve into the canonical integration platform.

Craft Marketplace must use the same security rules and connector registry.

Do not maintain:

one Global connector engine
+
one unrelated Marketplace connector engine.


======================================================================
ARCHITECTURAL RULE #3
======================================================================

External connectivity must be truthful.

A provider may only be marked:

AVAILABLE / CONNECTABLE

if a real adapter exists.

If no real adapter exists:

mark it:

PLANNED

or:

ADAPTER_NOT_AVAILABLE

Do NOT simulate a successful external connection.


======================================================================
KNOWN CURRENT EXTERNAL CONNECTOR STATE
======================================================================

At prompt preparation time:

MarketplaceConnectorRegistry is empty by design.

Shopee, TikTok Shop, Tokopedia etc. therefore have NO real adapter.

Existing Craft marketplace API sync intentionally reports that connector
sync is unavailable.

Preserve this honesty.

Do NOT invent marketplace API contracts.


======================================================================
PRE-FLIGHT REPOSITORY INSPECTION
======================================================================

Before editing inspect at minimum:

git status
git log
actual branch

uni-nexus.sql

UNI-NEXUS All Modules Description Generated by ChatGPT.md

src/App.tsx
Sidebar.tsx
Header.tsx
PermissionGate.tsx
AuthContext
WorkspaceContext

server/src/routes/index.ts
server/src/config/env.ts
root package.json
server/package.json

server/src/shared/integrations/**
server/src/modules/craft-marketplace/**
src/pages/craft/marketplace/**
src/services/api/craft-marketplace.api.ts
src/types/craft-marketplace.ts

server/src/shared/audit/**
server/src/shared/notifications/**
server/src/shared/settings/**
server/src/shared/automation/**
server/src/modules/automations/**
server/src/modules/calendar/**
server/src/modules/documents/**
server/src/modules/reports/**
server/src/modules/users/**

Search entire repo for:

integrations
integration_sync_logs
integration_type
provider_name
config_json
last_sync_at
MarketplaceConnectorRegistry
MarketplaceConnector
testIntegration
syncIntegration
requiredEnvironmentVariables
process.env
secret
token
password
api_key
access_token
refresh_token
private_key
webhook
fetch(
http://
https://
settings.manage
craft.marketplace.read
craft.marketplace.write
craft.marketplace.sync
window.alert
window.confirm
window.prompt

Do not trust this prompt blindly if newer main differs.


======================================================================
DATABASE DUMP — SOURCE OF TRUTH
======================================================================

Inspect latest:

uni-nexus.sql

Current known dump was generated:

Sep 01, 2026 03:29 AM

and already contains the Settings reconciliation.

Confirm current dump still contains:

integrations
integration_sync_logs

and confirm whether secure integration secret storage now exists.

At prompt preparation time it DOES NOT exist.


======================================================================
EXPECTED DATABASE CHANGE
======================================================================

Unlike several previous Global modules, this module is expected to require
a small, justified database reconciliation.

Expected migration:

server/migrations/012_global_integrations.sql

ONLY if current main still lacks the required secure secret storage and
Global Integration permissions.


======================================================================
DO NOT RECREATE EXISTING TABLES
======================================================================

Migration 012 must NOT recreate:

integrations
integration_sync_logs

Those are canonical existing tables.


======================================================================
WHY SECURE SECRET STORAGE IS REQUIRED
======================================================================

The canonical integrations.config_json schema explicitly states:

DO NOT STORE RAW SECRETS THERE.

Therefore:

API keys
access tokens
refresh tokens
private keys
client secrets
WhatsApp access tokens
Google service-account private keys

must NEVER be persisted in:

integrations.config_json
system_settings
audit_logs
integration_sync_logs
frontend localStorage
frontend sessionStorage
URLs
query strings


======================================================================
ENCRYPTED SECRET VAULT
======================================================================

If no reusable encrypted secret primitive exists in the actual current repo,
implement one.

Recommended table:

integration_secrets

Recommended fields:

id BIGINT UNSIGNED AUTO_INCREMENT

integration_id BIGINT UNSIGNED NOT NULL

secret_name VARCHAR(100) NOT NULL

key_version SMALLINT UNSIGNED NOT NULL DEFAULT 1

ciphertext LONGBLOB NOT NULL

iv VARBINARY(12) NOT NULL

auth_tag VARBINARY(16) NOT NULL

created_by BIGINT UNSIGNED NULL

updated_by BIGINT UNSIGNED NULL

created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3)

updated_at DATETIME(3) NOT NULL
DEFAULT CURRENT_TIMESTAMP(3)
ON UPDATE CURRENT_TIMESTAMP(3)

Unique:

(integration_id, secret_name)

FK integration:

ON DELETE CASCADE

User FK:

ON DELETE SET NULL

Adapt names to current conventions if appropriate.


======================================================================
SECRET CRYPTOGRAPHY
======================================================================

Use:

AES-256-GCM

with:

crypto.randomBytes()

random 12-byte IV per encryption

authentication tag verification

Never reuse IV.

Never use:

AES-ECB
plain AES-CBC without authentication
base64 as "encryption"
XOR
JWT_SECRET as the credential encryption key


======================================================================
MASTER SECRET KEY
======================================================================

Introduce a dedicated environment variable such as:

INTEGRATION_SECRET_KEY

Use a cryptographically strong 32-byte key encoded safely, e.g. base64.

Validate it.

Do not provide an insecure production default.

If secret operations are requested while the key is missing:

FAIL CLOSED

with a controlled configuration error.


======================================================================
LOCAL DEVELOPMENT KEY
======================================================================

If the local project has a gitignored .env:

Claude may generate a cryptographically random development
INTEGRATION_SECRET_KEY for the current machine.

Before doing this:

verify .env is ignored by Git.

Never:

commit it
print it
include it in logs
include it in final response

Update .env.example if one exists using only a placeholder.


======================================================================
SECRET AAD
======================================================================

Use AES-GCM Additional Authenticated Data containing stable identity such as:

organization_id
integration_id
secret_name
key_version

This helps prevent ciphertext from being swapped between integrations.


======================================================================
SECRET SERVICE
======================================================================

Create a neutral service such as:

server/src/shared/integrations/integration-secret.service.ts

Responsibilities:

set/update secret
read/decrypt secret internally
hasSecret
delete secret
deleteAllSecrets
redacted metadata
encryption
decryption
key validation

NO controller should decrypt secrets directly.


======================================================================
SECRET API RESPONSE RULE
======================================================================

The API must NEVER return the secret value.

Return only metadata such as:

{
  configured: true,
  updated_at: ...
}

Never return:

plaintext
ciphertext
iv
auth_tag
private key
access token


======================================================================
SECRET UPDATE UX
======================================================================

Frontend semantics:

blank secret input:
preserve current saved secret

new value:
replace secret

explicit "Hapus kredensial":
remove secret after confirmation

Never pre-fill a password field with the stored secret.


======================================================================
LEGACY SECRET REDACTION
======================================================================

Current Marketplace config_json historically accepted arbitrary config.

Add defensive recursive redaction.

Never return or Audit keys that look like credentials, including patterns
such as:

password
secret
token
access_token
refresh_token
api_key
apikey
private_key
client_secret
authorization
credential

Provider schemas must determine legitimate secret fields.

Do not depend only on string matching for new records.


======================================================================
GLOBAL INTEGRATION PERMISSIONS
======================================================================

At prompt preparation time no dedicated Global Integrations permissions
exist.

Introduce:

integrations.read

integrations.manage

integrations.sync


======================================================================
PERMISSION MEANING
======================================================================

integrations.read

View Global Integration Center, provider catalog, connection metadata and
sanitized history.

integrations.manage

Create/edit/enable/disable connections and update/remove credentials.

integrations.sync

Test a connection and perform an actual supported provider sync/action.


======================================================================
DEFAULT ROLE GRANTS
======================================================================

Do NOT grant these permissions to all users.

For backward-compatible initial reconciliation:

roles currently granted:

settings.manage

may inherit:

integrations.read
integrations.manage
integrations.sync

using permission CODE joins, not hard-coded numeric permission IDs.

This provides admin access while still allowing User Management to refine
role permissions later.


======================================================================
DOMAIN PERMISSION LAYERING
======================================================================

A Global Integration permission does NOT bypass domain permission.

Example Craft Marketplace connection:

Read requires:

integrations.read

AND

Craft BU access

AND

craft.marketplace.read

Manage requires:

integrations.manage

AND

Craft BU access

AND

craft.marketplace.write

Test/Sync requires:

integrations.sync

AND

Craft BU access

AND

craft.marketplace.sync


======================================================================
ORGANIZATION-SCOPE CONNECTION
======================================================================

A truly organization-global provider may have:

business_unit_id = NULL

Example:

organization Google Drive
organization WhatsApp sender
organization-level external API

Access still requires matching:

organization_id

plus Global Integration permission.


======================================================================
BUSINESS UNIT CONNECTION
======================================================================

Workspace-specific integration must belong to:

CRAFT
or
STUDIO

resolved by:

organization_id + stable BU code

Never trust client-supplied numeric business_unit_id.


======================================================================
ORGANIZATION ISOLATION
======================================================================

Every Global Integrations query must be constrained by:

authenticated actor.organization_id

Never accept organization_id from:

body
query
URL
config_json


======================================================================
BUSINESS UNIT ISOLATION
======================================================================

For BU-scoped connection:

check:

user_business_units.can_access = 1

Do not infer access only from permission strings.


======================================================================
MIGRATION 012 SAFETY
======================================================================

If migration is needed:

make it additive and safe.

It may:

CREATE TABLE IF NOT EXISTS integration_secrets

INSERT IGNORE permission rows

INSERT IGNORE role grants via permission code joins

add useful missing indexes only when necessary

Do not:

drop integrations
drop integration_sync_logs
rename domain tables
delete current integration records
truncate anything


======================================================================
INDEX REVIEW
======================================================================

Inspect query plans/current indexes.

Only add indexes that Global Integration Center actually needs.

Possible useful patterns include:

organization_id
business_unit_id
integration_type
status_code

Do not add redundant indexes blindly.


======================================================================
DDL EXECUTION RULE
======================================================================

Because Claude Code runs locally:

first inspect actual DB identity using current configuration.

Confirm DB is the expected UNI-NEXUS development database.

Before DDL:

inspect INFORMATION_SCHEMA.

Print the planned structural changes WITHOUT printing secrets.

Only then apply the additive migration if safe and necessary.

Do not run destructive DDL.

If DB connection cannot be confirmed:

do NOT guess.

Create the correct migration and report that live DB reconciliation could
not be performed.


======================================================================
uni-nexus.sql RULE
======================================================================

Do NOT manually edit the SQL dump.

Do not patch it as text.

If live DB reconciliation is successfully applied:

the owner can export the fresh phpMyAdmin dump afterward.

Final response must clearly say whether:

LIVE DB MIGRATION APPLIED: YES/NO

and whether:

uni-nexus.sql MODIFIED BY AGENT: NO


======================================================================
PROVIDER REGISTRY
======================================================================

Build one canonical provider registry, e.g.:

server/src/shared/integrations/integration-provider.registry.ts

It must define every provider that the UI exposes.


======================================================================
PROVIDER DEFINITION
======================================================================

Each provider definition should contain structured metadata such as:

code

displayName

integrationType

description

category

availability:
available | planned

allowedScopes:
organization | craft | studio

capabilities

public config fields

secret fields

authentication mode

required domain permissions

test support

sync support

icon/category metadata if useful

Never let arbitrary client payload define connector capabilities.


======================================================================
CANONICAL CONNECTOR INTERFACE
======================================================================

Create a neutral interface similar to:

IntegrationConnector

with explicit supported methods such as:

testConnection()

sync?()

disconnect?()

provider metadata

Never use:

eval
dynamic require from user input
arbitrary JS files
runtime code strings


======================================================================
CURRENT MARKETPLACE CONNECTOR COMPATIBILITY
======================================================================

Refactor:

MarketplaceConnectorRegistry

toward the canonical provider/connector platform.

Existing Craft Marketplace routes must continue functioning.

A compatibility facade is acceptable.

Do not break:

/api/v1/craft/marketplace/integrations

or:

/app/craft/marketplace/integrations


======================================================================
PROVIDER CATALOG — REQUIRED
======================================================================

At minimum the Global UI should represent these categories clearly:

Google Workspace

Messaging

Marketplace

Payment

API / Webhook

Other


======================================================================
GOOGLE PROVIDERS
======================================================================

Implement real adapter code for:

Google Drive
Google Sheets
Google Calendar

IF it can be done reliably using official Google APIs without inventing
contracts.

Recommended internal deployment auth for this UNI-NEXUS version:

Google Service Account credentials stored encrypted in integration_secrets.

Public config examples:

Google Drive:
folder_id

Google Sheets:
spreadsheet_id

Google Calendar:
calendar_id

Secret:

service_account_json

Do not store service-account JSON in config_json.


======================================================================
GOOGLE DEPENDENCY
======================================================================

Using the official:

googleapis

package is acceptable.

Add it only to the backend if actually used.

Do not add random unofficial Google wrappers.


======================================================================
GOOGLE CONNECTION TESTS
======================================================================

A connection test must be side-effect free.

Examples:

Drive:
retrieve/list configured folder metadata

Sheets:
retrieve configured spreadsheet metadata

Calendar:
retrieve configured calendar metadata

Do NOT create:

test files
test spreadsheets
test calendar events

just to verify credentials.


======================================================================
GOOGLE "SYNC" SEMANTICS
======================================================================

Do not invent a sync meaning.

If this module does not yet have a canonical mapping such as:

UNI-NEXUS Document Center ↔ Drive
UNI-NEXUS Report Center ↔ Sheets
UNI-NEXUS Calendar ↔ Google Calendar

then the provider may support:

connection_test

while:

sync = unavailable

That is acceptable and truthful.


======================================================================
DO NOT BUILD DUPLICATE DOMAIN SYNCHRONIZATION
======================================================================

Do not copy all Document Center files to Drive automatically.

Do not copy all Calendar events to Google automatically.

Do not export all Reports to Sheets automatically.

unless a precise mapping, idempotency model, ownership model, permission
model and test suite are implemented.

Connection capability is different from domain synchronization.


======================================================================
WHATSAPP CLOUD API
======================================================================

Implement a real:

WhatsApp Cloud API

connection adapter if practical using official Meta Graph API behavior.

Possible public config:

phone_number_id
business_account_id
graph_api_version

Secrets:

access_token
app_secret if required for webhook verification
verify_token if inbound webhook is implemented


======================================================================
WHATSAPP TEST
======================================================================

Connection test must NOT send a WhatsApp message.

Use a safe metadata/read operation to verify the configured number/account
when possible.

Never send test messages to a real user automatically.


======================================================================
WHATSAPP MESSAGE SENDING
======================================================================

Do NOT expose arbitrary "Send WhatsApp" functionality merely to prove the
integration works.

Messaging automation should be introduced only through a controlled action
registry and explicit recipient/template rules.

This task does not need to invent that workflow.


======================================================================
MARKETPLACE PROVIDERS
======================================================================

Represent important marketplace providers in the catalog if appropriate,
for example:

Shopee
TikTok Shop
Tokopedia

BUT:

if no official adapter exists in repository:

availability = planned

Test button disabled.

Sync button disabled.

Explain:

"Adapter API belum tersedia / belum dikonfigurasi."


======================================================================
DO NOT FABRICATE MARKETPLACE APIs
======================================================================

Do not guess:

Shopee signing
TikTok Shop endpoints
Tokopedia private contracts

from memory merely to make a green button.

Existing manual marketplace import must remain the functioning fallback.


======================================================================
PAYMENT PROVIDERS
======================================================================

Provide a Payment category/provider architecture.

Do not pretend a payment gateway is connected unless a verified adapter is
implemented.

Do not implement money movement merely as an Integration Center demo.

Payment initiation/capture/refund is a high-impact domain operation and
belongs to an explicit payment workflow.


======================================================================
CUSTOM API / GENERIC WEBHOOK
======================================================================

Do NOT casually implement arbitrary user-supplied URLs.

That can create SSRF.

A "Custom API" or "Generic Webhook" provider may be:

PLANNED

for this release.


======================================================================
IF GENERIC HTTP IS IMPLEMENTED
======================================================================

Only do this if you can fully harden it.

Must include:

HTTPS by default

block localhost

block loopback IPs

block RFC1918 private networks

block link-local

block cloud metadata addresses

block file://

block ftp://

block non-http schemes

DNS resolution validation

redirect restrictions

request timeout

response-size limit

header allowlist

no arbitrary Authorization response exposure

no credential logging

If this cannot be implemented comprehensively:

DO NOT IMPLEMENT GENERIC HTTP.


======================================================================
GLOBAL BACKEND MODULE
======================================================================

Create a dedicated neutral module such as:

server/src/modules/integrations/

Suggested files:

integrations.routes.ts
integrations.controller.ts
integrations.service.ts
integrations.repository.ts
integrations.schema.ts
integrations.types.ts
integrations-access.service.ts

Provider/connector primitives remain under:

server/src/shared/integrations/


======================================================================
GLOBAL API
======================================================================

Create:

/api/v1/integrations

Recommended endpoints:

GET /overview

GET /meta

GET /providers

GET /connections

POST /connections

GET /connections/:id

PATCH /connections/:id

POST /connections/:id/credentials

DELETE /connections/:id/credentials/:secretName

POST /connections/:id/test

POST /connections/:id/sync

POST /connections/:id/enable

POST /connections/:id/disable

POST /connections/:id/disconnect

GET /logs

GET /logs/:id

Adapt exact REST shapes to project conventions.


======================================================================
NO SECRET IN QUERY PARAMETERS
======================================================================

Credential mutation must use request body.

Never place credentials in:

URL
query string
redirect parameter


======================================================================
CONNECTION CREATION
======================================================================

Create flow:

select provider

select allowed scope

resolve actual BU server-side

validate public config

save connection metadata

save credentials separately encrypted

status remains:

not_connected

until a real successful test


======================================================================
CONNECTION CODE
======================================================================

Generate collision-safe connection codes.

Do not use a shared static temporary:

TMP

Use either:

TMP-<UUID>

then canonical:

INT-000001

or another current project-consistent collision-safe design.


======================================================================
STATUS MODEL
======================================================================

Use the existing canonical values:

not_connected

connected

error

disabled

planned


======================================================================
STATUS SEMANTICS
======================================================================

planned:

provider adapter does not exist

not_connected:

provider available but credentials/config not validated

connected:

real test succeeded

error:

last meaningful provider operation failed

disabled:

administrator intentionally disabled the connection


======================================================================
DO NOT MARK CONNECTED ON SAVE
======================================================================

Saving credentials does NOT mean connected.

Only a successful real connection test may transition to:

connected


======================================================================
DISCONNECT
======================================================================

Disconnect must:

remove encrypted credentials
set status not_connected
retain connection metadata
retain safe historical sync logs

Do not delete operational history.


======================================================================
DISABLE
======================================================================

Disable keeps credentials but prevents:

test
sync
connector usage
automation connector execution

until explicitly enabled again.


======================================================================
RE-ENABLE
======================================================================

Enable must not assume credentials still work.

Recommended status:

not_connected

until the connection is tested again.


======================================================================
OPTIMISTIC CONCURRENCY
======================================================================

Use current updated_at or another existing safe mechanism to prevent two
administrators from silently overwriting the same connection.

If stale:

409

INTEGRATION_VERSION_CONFLICT

Do not add version_no column unless genuinely necessary.


======================================================================
GLOBAL FRONTEND ROUTES
======================================================================

Implement:

/app/integrations

/app/integrations/connections

/app/integrations/connections/new

/app/integrations/connections/:id

/app/integrations/connections/:id/edit

/app/integrations/providers

/app/integrations/history

/app/integrations/history/:id


======================================================================
GLOBAL FRONTEND STRUCTURE
======================================================================

Create something like:

src/pages/global/integrations/

Integrations.tsx

pages/components as appropriate:

IntegrationOverviewPage
IntegrationConnectionsPage
IntegrationConnectionDetailPage
IntegrationConnectionEditorPage
IntegrationProvidersPage
IntegrationHistoryPage
IntegrationLogDetailPage

components:

IntegrationHeader
IntegrationStatusBadge
ProviderBadge
ScopeBadge
CapabilityBadge
ConnectionHealthCard
CredentialEditor
ProviderConfigForm
ConnectionTestPanel
SyncHistoryTable
EmptyState

Keep files cohesive.


======================================================================
GLOBAL API CLIENT
======================================================================

Create:

src/services/api/integrations.api.ts

and:

src/types/integrations.ts

Use strong types.

Avoid core API `any`.


======================================================================
APP ROUTE
======================================================================

Add a real route for:

/app/integrations/*

The page must no longer fall into:

"Sedang Dalam Pengembangan"


======================================================================
SIDEBAR
======================================================================

Current Global Sidebar Integrasi item is effectively unguarded.

Protect it using:

integrations.read


======================================================================
GLOBAL PATH BEHAVIOR
======================================================================

Header already treats:

/app/integrations

as a Global route.

Preserve this.

Switching:

Craft → Studio

or:

Studio → Craft

while inside Integrations must keep the user in:

/app/integrations...


======================================================================
OVERVIEW PAGE
======================================================================

Show useful operational metrics such as:

Total connections

Connected

Need attention / error

Not connected

Disabled

Planned providers

Recent connection tests

Recent sync operations

Recent failures


======================================================================
CONNECTION HEALTH
======================================================================

Do not invent online heartbeat.

Health should be derived from real evidence:

status_code

latest test log

latest sync log

last_sync_at

latest failure

provider availability

credential configuration status


======================================================================
CONNECTION LIST
======================================================================

Show:

provider

display name

scope

workspace

type/category

status

capabilities

last tested

last sync

latest result

creator/update information if available


======================================================================
CONNECTION FILTERS
======================================================================

Support:

search

provider/category

scope

workspace

status

capability


======================================================================
PROVIDER CATALOG UI
======================================================================

Show all registered provider definitions.

Separate clearly:

Tersedia

Direncanakan

For each provider show:

name
description
category
allowed scope
authentication type
capabilities
connection-test support
sync support


======================================================================
PLANNED PROVIDER UX
======================================================================

A planned provider should NOT have an active:

Connect

Test

Sync

button.

Show:

"Adapter belum tersedia"

with clear explanation.


======================================================================
CONNECTION WIZARD
======================================================================

Create a guided wizard:

Step 1:
Pilih Provider

Step 2:
Pilih Scope

Step 3:
Konfigurasi

Step 4:
Kredensial

Step 5:
Review

Step 6:
Simpan & Uji Koneksi


======================================================================
PROVIDER-DRIVEN FORMS
======================================================================

The provider registry must drive:

public config fields

secret fields

labels

input types

required/optional state

help text

Do not implement one giant provider-specific switch across many frontend
pages if registry metadata can safely drive the form.


======================================================================
SECRET FIELD UX
======================================================================

Secret fields use:

type=password

Never display existing value.

If configured:

show only:

"Sudah dikonfigurasi"

and updated timestamp if useful.


======================================================================
COPY / REVEAL SECRET
======================================================================

Do NOT add:

Reveal stored secret

Copy stored token

The backend must not provide such an endpoint.


======================================================================
CONNECTION DETAIL
======================================================================

Show:

integration code

provider

scope

workspace

status

safe public configuration

credential configuration state

capabilities

connection test history

sync history

last sync

created/updated

safe Audit link if useful


======================================================================
TEST CONNECTION
======================================================================

Test is a real provider operation.

Requires:

integrations.sync

and domain permissions where applicable.

The test must:

record a log

record start/end

store sanitized metadata

update status appropriately

never leak credentials


======================================================================
SYNC EXECUTION
======================================================================

Sync button must exist ONLY when:

connector is available

AND

connector defines the requested sync capability

AND

connection is connected

AND

actor has required permissions


======================================================================
SYNC LOGGING
======================================================================

Use canonical:

integration_sync_logs

For every meaningful operation record:

integration_id

sync_type

direction

status_code

started_at

finished_at

records_processed

records_success

records_failed

safe error_message

safe metadata


======================================================================
CONNECTION TEST LOG
======================================================================

Connection tests may use:

sync_type = 'connection_test'

or another stable canonical code.

Do not create a second testing-log table.


======================================================================
LAST_SYNC_AT
======================================================================

Update:

integrations.last_sync_at

only for an actual successful/partial synchronization.

Do not update it merely because:

connection test succeeded.


======================================================================
ERROR SANITIZATION
======================================================================

External provider errors may contain sensitive payloads.

Sanitize before storing or returning.

Never persist:

Authorization header

access token

refresh token

service-account private key

full credential JSON

raw provider response containing secrets


======================================================================
SAFE PROVIDER METADATA
======================================================================

Safe examples:

Google spreadsheet title

Google calendar summary

Google Drive folder name

WhatsApp verified display name

provider request ID

record counts

Do not store huge external payloads in metadata.


======================================================================
NETWORK TIMEOUT
======================================================================

All provider network calls require a reasonable timeout.

Do not allow a request to hang indefinitely.


======================================================================
FIXED PROVIDER HOSTS
======================================================================

Real built-in adapters must use fixed trusted provider endpoints.

Do not accept a custom API base URL for Google or WhatsApp.


======================================================================
AUDIT INTEGRATION
======================================================================

Use canonical:

AuditService

module_code:

integrations

Audit meaningful mutations such as:

integration.create

integration.update

integration.credentials.update

integration.credentials.remove

integration.test

integration.sync

integration.enable

integration.disable

integration.disconnect


======================================================================
AUDIT SECRET RULE
======================================================================

Audit old/new values must NEVER contain secret material.

Allowed:

{
  credential_fields_changed: ["access_token"],
  credential_configured: true
}

Forbidden:

{
  access_token: "..."
}


======================================================================
NOTIFICATION PERMISSION MAPPING
======================================================================

If Integration notifications are emitted, register:

integrations -> integrations.read

in the canonical module read-permission mapping.

Do not notify users who cannot open Integrations.


======================================================================
FAILURE NOTIFICATION
======================================================================

Do NOT spam users on every manual connection test failure.

If a future automated sync fails repeatedly, a deduplicated warning may be
appropriate.

For this task, manual test result shown directly in UI is sufficient.


======================================================================
CRAFT MARKETPLACE REFACTOR
======================================================================

Existing Craft Marketplace Integration must remain operational.

Refactor its integration lifecycle to use canonical shared services where
appropriate.

Preserve:

sales_channel_id

integration_type='marketplace'

manual import functionality

Craft permissions

Craft Audit semantics


======================================================================
MARKETPLACE CONFIG SECURITY
======================================================================

Marketplace config_json must become strictly non-secret.

Reject or extract credential fields into the encrypted secret vault.

Never allow:

access_token
secret_key
partner_key
refresh_token

to be stored in config_json.


======================================================================
MARKETPLACE FRONTEND COMPATIBILITY
======================================================================

Existing:

/app/craft/marketplace/integrations

must continue working.

It may:

use compatibility APIs

or

deep-link to the Global connection detail.

Do not remove useful Craft-specific context.


======================================================================
MARKETPLACE SYNC ACTION
======================================================================

Pusat Otomasi already contains a marketplace.sync action.

Make sure it uses the same canonical connector capability.

If the selected Marketplace connector has no sync implementation:

skip/fail with a controlled:

INTEGRATION_CAPABILITY_UNAVAILABLE

Do not fake a sync.


======================================================================
AUTOMATION SAFETY
======================================================================

Do NOT add a generic:

webhook.send

api.request

javascript.execute

action to Automation merely because Integrations now exists.

Trusted provider-specific actions can be added later.


======================================================================
GOOGLE CALENDAR VS UNI-NEXUS CALENDAR
======================================================================

Do not create automatic Google Calendar synchronization unless it is
complete.

If implemented, it must use:

source ownership
stable external event ID mapping
idempotency
loop prevention
permission checks

Otherwise:

Google Calendar connection/test is complete,
while calendar sync capability remains unavailable.


======================================================================
GOOGLE DRIVE VS DOCUMENT CENTER
======================================================================

Document Center remains UNI-NEXUS canonical internal document registry.

Connecting Google Drive must not silently replace:

StorageService
DocumentRegistryService

unless a clearly defined synchronization layer is implemented.


======================================================================
GOOGLE SHEETS VS REPORT CENTER
======================================================================

Report Center remains canonical.

Do not silently replace report exports with Google Sheets.

A Google Sheets connector may validate/read spreadsheet metadata while
future export/sync capabilities remain explicit.


======================================================================
SETTINGS BOUNDARY
======================================================================

Do not store provider credentials in:

system_settings

Settings may contain non-secret integration defaults later, but credentials
belong in the Integration secret vault.


======================================================================
NO SECRETS IN BROWSER STORAGE
======================================================================

Do not persist credentials in:

localStorage
sessionStorage
IndexedDB

Credential form state should live only in transient React state until
submitted.


======================================================================
NO SECRETS IN URL
======================================================================

Do not include a secret in:

query params
hash fragment
route params
navigation state that can become history


======================================================================
NO NATIVE DIALOGS
======================================================================

No:

window.alert
window.confirm
window.prompt

Use project:

Modal
ConfirmDialog
or equivalent.


======================================================================
DANGEROUS ACTION CONFIRMATION
======================================================================

Use custom confirmation for:

Disable

Disconnect

Delete credentials

Manual sync if it can modify external/internal state


======================================================================
RESPONSIVE DESIGN
======================================================================

Follow authenticated UNI-NEXUS visual language:

warm background

cream/white cards

yellow accent

charcoal text

--nexus-* variables

existing Card/Button/Badge patterns

lucide-react


======================================================================
MOBILE
======================================================================

No page-level horizontal overflow.

On mobile:

cards stack

filters remain usable

tables have local horizontal scroll only when necessary

credential form remains usable

dialog remains inside viewport


======================================================================
ACCESSIBILITY
======================================================================

Every credential/config input needs a label.

Status must not be communicated by color alone.

Dialogs:

role=dialog
aria-modal=true

Keyboard:

Escape closes dialog where safe
Tab order sensible
buttons have accessible names


======================================================================
LOADING / EMPTY / ERROR STATES
======================================================================

Every page needs:

loading

empty

error

retry

No blank screen.


======================================================================
CONTROLLED API ERRORS
======================================================================

Use codes such as:

INTEGRATION_NOT_FOUND

INTEGRATION_FORBIDDEN

INTEGRATION_PROVIDER_UNKNOWN

INTEGRATION_PROVIDER_PLANNED

INTEGRATION_SCOPE_INVALID

INTEGRATION_CREDENTIALS_REQUIRED

INTEGRATION_SECRET_KEY_NOT_CONFIGURED

INTEGRATION_CONNECTION_FAILED

INTEGRATION_NOT_CONNECTED

INTEGRATION_DISABLED

INTEGRATION_CAPABILITY_UNAVAILABLE

INTEGRATION_VERSION_CONFLICT

INTEGRATION_SYNC_FAILED

Do not expose MySQL errors.


======================================================================
SMOKE TEST
======================================================================

Create:

server/scripts/smoke-integrations.ts

Add package script:

"smoke:integrations":
"tsx scripts/smoke-integrations.ts"


======================================================================
NO REAL EXTERNAL NETWORK IN SMOKE TEST
======================================================================

The smoke test must NOT depend on:

Google
Meta
Shopee
TikTok
Tokopedia
payment provider

being reachable.


======================================================================
TEST CONNECTOR
======================================================================

Use dependency injection or a temporary mock connector registered only
inside test execution.

Do NOT expose it in normal production provider catalog.


======================================================================
SMOKE TEST MUST COVER
======================================================================

AUTH / RBAC

1. unauthenticated API denied.

2. integrations.read can read.

3. no integrations.read denied.

4. manage permission required for mutation.

5. sync permission required for test/sync.

6. domain-specific Marketplace permission additionally required.


ORGANIZATION ISOLATION

7. organization A cannot see organization B connection.

8. cross-org detail returns not found/forbidden safely.

9. cross-org logs inaccessible.

10. client-supplied organization_id ignored/rejected.


BU ISOLATION

11. BU-scoped integration resolves within organization.

12. user without user_business_units access cannot access it.

13. organization-scoped integration does not require fake BU.

14. numeric BU from client cannot escape organization.


CONNECTION CREATE

15. available provider connection can be created.

16. planned provider cannot pretend connected.

17. collision-safe integration_code generated.

18. public config validated.

19. secret config rejected from config_json.


SECRET VAULT

20. secret can be encrypted.

21. decrypt returns exact value internally.

22. database does not contain plaintext secret.

23. API response does not contain plaintext.

24. API response does not contain ciphertext/IV/auth_tag.

25. Audit does not contain plaintext.

26. integration_sync_logs do not contain plaintext.

27. replacing secret works.

28. blank frontend-style update preserves existing secret.

29. secret deletion works.

30. disconnect deletes all secrets.


KEY SAFETY

31. missing INTEGRATION_SECRET_KEY prevents secret operation safely.

32. invalid key rejected.

33. authentication-tag failure does not return garbage plaintext.


PROVIDER CATALOG

34. provider registry returns stable metadata.

35. available providers correctly marked.

36. planned providers correctly marked.

37. provider capabilities accurate.

38. arbitrary provider code rejected.


STATUS

39. saved connection remains not_connected.

40. successful mock test -> connected.

41. failed mock test -> error.

42. disable -> disabled.

43. disabled connection cannot sync.

44. re-enable requires retest / becomes not_connected.

45. disconnect -> not_connected.


TEST LOG

46. connection test creates integration_sync_logs row.

47. safe metadata stored.

48. finished_at set.

49. failure error sanitized.


SYNC

50. connector without sync capability rejected.

51. connector with mock sync executes.

52. running log becomes success.

53. record counts stored.

54. last_sync_at updated only on actual successful sync.

55. testConnection does not change last_sync_at.

56. failed sync recorded.

57. disabled integration cannot sync.


CONCURRENCY

58. stale connection update returns 409.

59. no silent overwrite.


CRAFT MARKETPLACE

60. existing marketplace list still works.

61. existing marketplace create/update still works.

62. config no longer accepts raw secret leakage.

63. manual import path still works.

64. absent marketplace connector remains planned/not configured.

65. fake API success is not returned.


AUDIT

66. create audited.

67. config update audited.

68. credential update audited without secret.

69. test audited.

70. sync audited.

71. disable audited.

72. disconnect audited.


CLEANUP

73. generated logs deleted.

74. generated secrets deleted.

75. generated integrations deleted.

76. generated Audit rows deleted.

77. fixture users/roles/BUs/org cleaned.

Use finally.


======================================================================
BROWSER ACCEPTANCE
======================================================================

Create:

server/scripts/browser-integrations.mjs

Add:

"browser:integrations":
"node scripts/browser-integrations.mjs"


======================================================================
BROWSER TEST
======================================================================

Use isolated fixture data.

Do NOT alter real integration records.


======================================================================
BROWSER ACCEPTANCE MATRIX
======================================================================

Verify real DOM/UI interaction for at least:

1. login.

2. Integrasi sidebar visible for authorized user.

3. Integrasi hidden for unauthorized user.

4. direct unauthorized route denied.

5. /app/integrations loads real page.

6. overview KPI loads.

7. provider catalog loads.

8. Available vs Planned distinction visible.

9. connection list loads.

10. filters work.

11. New Connection wizard opens.

12. provider selection works.

13. scope selection works.

14. public config form changes per provider.

15. secret field appears as password input.

16. save creates not_connected connection.

17. secret is never rendered back.

18. configured credential shows "Sudah dikonfigurasi".

19. Connection Detail loads.

20. Test Connection uses custom action UI.

21. successful fixture test shows connected.

22. failed fixture test shows controlled error.

23. history row appears.

24. Log Detail is sanitized.

25. Disable confirmation works.

26. disabled connection cannot test/sync.

27. enable works.

28. Disconnect confirmation works.

29. credentials become unconfigured.

30. planned provider has no functional Connect/Sync button.

31. Marketplace compatibility page still loads.

32. workspace switch on /app/integrations remains on global route.

33. mobile viewport no page overflow.

34. dialog keyboard behavior works.

35. no window.alert.

36. no window.confirm.

37. no window.prompt.

38. no unhandled console exception.

39. fixture cleanup succeeds.


======================================================================
REAL GOOGLE / WHATSAPP TESTS
======================================================================

Do NOT require real credentials for automated CI/smoke.

If real credentials already exist locally and are intentionally configured,
Claude may manually test them ONLY if doing so is clearly non-destructive.

Never print the credentials.

If no credentials exist:

report:

REAL PROVIDER TEST: SKIPPED — CREDENTIALS NOT CONFIGURED

That is NOT a failure of the module.


======================================================================
REGRESSION TESTS
======================================================================

Inspect actual server/package.json first.

Run relevant existing tests that truly exist.

At minimum:

npm --prefix server run smoke:integrations

npm --prefix server run smoke:craft-marketplace

npm --prefix server run smoke:automations-center

npm --prefix server run smoke:craft-automations

npm --prefix server run smoke:audit

npm --prefix server run smoke:settings

npm --prefix server run smoke:notifications

If provider/Calendar/Document code was touched:

smoke:calendar-tasks

smoke:documents

smoke:reports

Run only scripts that exist.


======================================================================
BROWSER REGRESSION
======================================================================

Run:

npm --prefix server run browser:integrations

and if affected:

browser:craft-marketplace

browser:automations-center

Do not claim PASS if not actually run.


======================================================================
ROOT VALIDATION
======================================================================

From repository root run:

npm run lint

npm run build

Fix every error introduced by this work.


======================================================================
NO TEST WEAKENING
======================================================================

Never make tests pass by:

granting all users permissions

removing authorization

disabling assertions

mocking production service accidentally

hardcoding production user IDs

catching errors silently

marking unavailable provider as connected


======================================================================
SECURITY REVIEW
======================================================================

Before completion explicitly verify:

no plaintext integration secret in DB config

no plaintext secret in Audit

no plaintext secret in sync logs

no secret in API responses

no secret in browser storage

no secret in URLs

no committed secret

no arbitrary SQL

no eval

no Function()

no shell execution

no arbitrary dynamic module loading

no uncontrolled SSRF

no cross-organization leak

no BU access bypass

no Global permission bypass

no domain permission bypass

no fake connection success

no fake sync success


======================================================================
SEARCH FOR SECRET LEAKS
======================================================================

Search changed code for:

access_token
refresh_token
client_secret
private_key
password
api_key
apikey
authorization
credential
config_json
console.log
console.error
AuditService
integration_sync_logs

Review every occurrence.


======================================================================
NO CONFIGURATION THEATER
======================================================================

For every provider/card/button displayed as functional ask:

"Can this current backend REALLY perform this operation?"

If NO:

disable it and label it planned/unavailable.

Never create a green UI that is not backed by real implementation.


======================================================================
EXTERNAL SERVICES ARE OPTIONAL AT RUNTIME
======================================================================

UNI-NEXUS must still start successfully even when:

Google credentials are absent

WhatsApp credentials are absent

marketplace APIs are absent

payment adapters are absent

Only the relevant integration connection should be unavailable.

The whole ERP must not fail startup.


======================================================================
FINAL DEVELOPMENT RESTART
======================================================================

After implementation and testing:

restart the UNI-NEXUS development environment.

Root package currently defines:

npm run dev

which runs:

Vite frontend

Express backend

AutomationWorker

together.

Procedure:

identify stale UNI-NEXUS processes

stop only processes belonging to this repository

do not kill unrelated machine processes

ensure required ports are free

from root:

npm run dev

keep it running if environment supports it


======================================================================
VERIFY AFTER RESTART
======================================================================

Verify:

frontend reachable

GET /api/v1/health

database connected

GET /api/v1/integrations/providers

responds for authorized user

/app/integrations

renders

Craft Marketplace page still loads

Pusat Otomasi still loads

exactly one AutomationWorker entrypoint is active


======================================================================
FINAL GIT CHECK
======================================================================

Run:

git branch --show-current

git status

git log -1 --oneline

git diff -- uni-nexus.sql


Expected:

branch main

no new branch

no accidental SQL dump hand-edit

migration 012 only if genuinely required


======================================================================
EXPECTED FINAL ARCHITECTURE
======================================================================

When complete UNI-NEXUS should have:

ONE Integration data model

ONE provider registry

ONE credential vault

ONE connection lifecycle

ONE sanitized sync-history model

ONE Global Integration Center

Craft Marketplace as a domain adapter over that platform

Provider capabilities that are truthful

No secret leakage

No fake marketplace APIs


======================================================================
FINAL ACCEPTANCE CRITERIA
======================================================================

The task is complete only when:

1. latest main inspected.

2. uni-nexus.sql inspected.

3. product Markdown inspected.

4. main only.

5. no branch created.

6. integrations table reused.

7. integration_sync_logs reused.

8. encrypted credential storage exists.

9. credentials use authenticated encryption.

10. dedicated encryption key used.

11. no insecure default key.

12. secrets never go into config_json.

13. secrets never returned by API.

14. secrets never go into Audit.

15. secrets never go into sync logs.

16. Global Integration permissions exist.

17. role seed is safe.

18. organization isolation enforced.

19. BU isolation enforced.

20. domain permission layering works.

21. /app/integrations implemented.

22. global route remains global on workspace switch.

23. sidebar permission fixed.

24. /api/v1/integrations implemented.

25. overview functional.

26. connection list functional.

27. provider catalog functional.

28. provider availability truthful.

29. connection wizard functional.

30. credential editor functional.

31. connection detail functional.

32. test connection functional.

33. test logs functional.

34. actual sync only shown when supported.

35. sync logs functional.

36. status lifecycle correct.

37. disconnect removes credentials.

38. disable prevents operations.

39. optimistic concurrency works.

40. Audit integration works.

41. Craft Marketplace compatibility preserved.

42. marketplace config secret leakage fixed.

43. manual marketplace import preserved.

44. absent marketplace API adapters remain planned.

45. Global Automation marketplace.sync remains safe.

46. real Google adapters implemented where feasible.

47. Google test operations non-destructive.

48. WhatsApp adapter implemented where feasible.

49. WhatsApp connection test sends no real message.

50. Payment provider is not fabricated.

51. generic webhook/API does not create SSRF vulnerability.

52. responsive desktop/tablet/mobile.

53. accessible forms/dialogs.

54. no native alert/confirm/prompt.

55. smoke:integrations passes.

56. craft-marketplace regression passes.

57. automation regression passes.

58. Audit regression passes.

59. relevant browser acceptance passes.

60. fixture cleanup passes.

61. npm run lint passes.

62. npm run build passes.

63. live DB migration result explicitly reported.

64. uni-nexus.sql not manually modified.

65. npm run dev restarted.

66. frontend reachable.

67. backend health green.

68. Integration API reachable.

69. one AutomationWorker active.

70. final branch remains main.


======================================================================
FINAL RESPONSE FORMAT
======================================================================

After ACTUALLY completing the work, report:

Starting HEAD

Final HEAD

Branch

BRANCH CREATED: NO

WORKED ONLY ON main: YES

Repository areas inspected

Database schema inspected

Migration created:
YES/NO

Migration file

LIVE DB MIGRATION APPLIED:
YES/NO

uni-nexus.sql MODIFIED BY AGENT:
NO

Permissions added

Secret vault implementation

Encryption algorithm

Environment variables added

Provider registry implemented

Available providers

Planned providers

Google adapter status

WhatsApp adapter status

Marketplace adapter status

Payment adapter status

Global API routes

Frontend routes

RBAC behavior

Organization isolation

BU isolation

Marketplace compatibility

Automation compatibility

Audit behavior

Connection lifecycle

Sync/log behavior

Security/redaction result

Files created

Important files modified

Dependencies added and why

Every test ACTUALLY run:
command + PASS/FAIL/SKIPPED + reason

Real external-provider test status

Browser acceptance result

Fixture cleanup result

npm run lint result

npm run build result

npm run dev restart result

frontend URL/result

/api/v1/health result

/api/v1/integrations/providers result

AutomationWorker instance count

git branch --show-current

git status

git log -1 --oneline


======================================================================
FINAL GOAL
======================================================================

Finish the last Global UNI-NEXUS module as a real, secure Integration
Control Plane.

The owner must be able to open:

Integrasi

and immediately understand:

- layanan apa yang tersedia
- layanan apa yang masih direncanakan
- koneksi mana yang sudah terhubung
- koneksi itu milik organisasi/Craft/Studio
- kredensial sudah dikonfigurasi atau belum
- apa kemampuan connector tersebut
- apakah koneksi benar-benar berhasil diuji
- kapan terakhir melakukan sinkronisasi
- apa yang gagal
- berapa record yang diproses
- apakah sebuah sync benar-benar tersedia
- apakah koneksi sedang disabled
- apakah credential sudah dihapus

without exposing:

passwords
tokens
API keys
private keys
provider secrets

and without inventing external API functionality that does not really
exist.

After this task, UNI-NEXUS should have all planned V1/V2 application modules
implemented and be ready for the owner's separate full-system testing phase.