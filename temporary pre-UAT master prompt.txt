# UNI-NEXUS Pre-UAT Completion and Gap Remediation

You are working directly inside the existing `Taqizdihar/uni-nexus` repository opened in VS Code.

Your task is to perform a complete pre-UAT remediation pass and finish every incomplete, placeholder-based, disconnected, or misleadingly exposed functionality that can reasonably be completed inside this repository.

Do not merely analyze the gaps or produce a plan. Inspect the current repository, implement the fixes, update the required database and application layers, run verification, and report the final result.

The formal manual UAT Spreadsheet will be created later. This task must make the application genuinely ready for that testing stage.

## 1. Repository context

UNI-NEXUS is a React + Vite frontend with a Node.js/Express/TypeScript backend and MySQL database.

Its functionality is divided into:

* Global UNI-NEXUS modules.
* Uni-Inside Craft.
* Uni-Inside Studio.
* Public authentication and account-lifecycle pages.
* Cross-module systems such as finance, documents, calendar, notifications, audit logging, integrations, automations, analytics, and reports.

Start by inspecting the actual current `HEAD`. Do not assume that the repository is still identical to any earlier audit or commit.

Important files and areas include, but are not limited to:

* `src/App.tsx`
* `src/components/layout/Sidebar.tsx`
* `src/components/layout/Header.tsx`
* `src/context/AuthContext.tsx`
* `src/context/WorkspaceContext.tsx`
* `src/pages/craft/Finance.tsx`
* `src/pages/craft/finance/`
* `src/pages/studio/finance/`
* `src/pages/global/UnifiedFinance.tsx`
* `src/pages/craft/Automations.tsx`
* `src/pages/global/automations/`
* `src/pages/studio/Automations.tsx`
* `src/pages/global/integrations/`
* `src/services/api/`
* `src/types/`
* `server/src/modules/craft-finance/`
* `server/src/modules/finance/`
* `server/src/modules/studio-finance/`
* `server/src/modules/craft-automations/`
* `server/src/modules/integrations/`
* `server/src/shared/finance/`
* `server/src/shared/automation/`
* `server/src/shared/integrations/`
* `server/src/shared/audit/`
* `server/src/shared/notifications/`
* `server/src/routes/index.ts`
* Existing migrations, schema snapshots, smoke tests, and browser acceptance scripts.

## 2. Primary objective

After this task:

1. Every known sidebar route must render a real, intentional page.
2. No known route may unintentionally fall into `PlannedModulePage`.
3. The six incomplete Craft Finance submenu pages must have genuine functionality.
4. The global search control must perform real permission-aware searches.
5. The user’s default workspace must be honored correctly.
6. Craft Orders must have consistent frontend and backend permission protection.
7. Craft Automation must have a proper Event Inbox page.
8. Integration availability must be honest, secure, and consistent.
9. Any additional incomplete or disconnected functionality discovered during the audit must be repaired.
10. The application must remain safe on an empty transactional database and must not rely on sample or hard-coded business data.
11. Relevant builds, automated tests, smoke tests, and browser acceptance checks must pass.

## 3. Non-negotiable constraints

* Preserve the existing UNI-NEXUS UI/UX, visual language, navigation structure, Indonesian interface text, components, design tokens, and responsive behavior.
* Do not redesign working pages.
* Do not replace existing canonical services with parallel or duplicate implementations.
* Do not introduce a second financial ledger, duplicate source of truth, or disconnected finance tables.
* Reuse existing global finance, Studio Finance, shared finance posting, audit, notification, calendar, document, and automation infrastructure.
* Preserve organization isolation and `business_unit_id` scoping.
* Never expose data from a module the authenticated user cannot read.
* All backend access control must remain authoritative. Hiding a button or sidebar item is not sufficient.
* Do not use mock business data in production UI.
* Do not insert permanent test data into the normal database.
* Do not run destructive database reset or cleanup scripts.
* Do not run migrations or destructive commands against a production database.
* Use a dedicated development/test database or safely isolated fixtures for automated verification.
* Preserve unrelated user changes in the working tree.
* Do not use `git reset`, destructive checkout operations, or discard unrelated changes.
* Do not commit or push unless explicitly requested.
* Do not mark a third-party provider as available unless a real, secure connector exists.
* Do not fabricate successful third-party integration behavior.
* Do not stop after producing an audit report. Implement every actionable code-level gap.

## 4. Phase A — Full pre-implementation audit

Before changing code, create an internal implementation matrix by comparing:

* Every item and submenu in `Sidebar.tsx`.
* Every route in `App.tsx`.
* Every child router inside Craft, Studio, Global Automations, and Integrations.
* Every frontend API client.
* Every backend route/controller/service.
* Every required database table and migration.
* Every permission.
* Existing smoke and browser acceptance tests.

Search the repository for at least:

* `Placeholder`
* `PlannedModulePage`
* `Dalam Pengembangan`
* `planned`
* `TODO`
* `FIXME`
* `not implemented`
* `coming soon`
* hard-coded mock records
* controls without handlers
* routes without matching pages
* frontend API methods without backend endpoints
* backend endpoints with no reachable UI
* sidebar paths that fall into wildcard routes
* misleading buttons or controls that do nothing
* permission mismatches
* business data that is calculated differently in separate modules

Do not treat a generic empty-state page as completed functionality.

A real empty state is acceptable only when the page is fully connected to its real data source and genuinely has zero records.

## 5. Known gap 1 — Complete all six Craft Finance submenu pages

The following routes currently use or previously used a generic placeholder and must become real pages:

1. `/app/craft/finance/income`
2. `/app/craft/finance/expenses`
3. `/app/craft/finance/profitability`
4. `/app/craft/finance/cash-flow`
5. `/app/craft/finance/budgets`
6. `/app/craft/finance/accounting`

Remove every remaining `FinancePlaceholderPage` reference for these routes.

Delete the placeholder component only if it becomes genuinely unused and can be safely removed.

### 5.1 Architectural requirements

Use the existing canonical finance architecture:

* `server/src/shared/finance/finance-posting.service.ts`
* Global Unified Finance services and tables.
* Existing Craft Finance services.
* Existing Studio Finance implementation where its patterns are reusable.
* Existing financial transactions, treasury accounts, categories, payments, journals, journal lines, budgets, and financial periods.
* Existing organization and business-unit scoping.
* Existing audit logging and idempotency conventions.

Craft Finance pages must be scoped strictly to the Craft business unit.

Do not copy Studio Finance queries and merely rename them without confirming their semantic correctness.

Do not create duplicate Craft-only ledger tables when the canonical shared financial tables already support business-unit scoping.

### 5.2 Income page

Implement a genuine Craft Income page with:

* Period filter.
* Search.
* Income category filter.
* Treasury account filter.
* Source filter where applicable.
* Pagination.
* KPI summary.
* Real posted-income records.
* Source code and source module visibility.
* Manual income entry for users with `craft.finance.write`, if supported by the current finance model.
* Proper validation, idempotency, journal posting, treasury balance update, and audit record.
* Safe reversal instead of deleting posted ledger entries.
* Clear distinction between sales payments, marketplace settlements, manual income, and reversals.
* Genuine empty, loading, error, and permission states.

### 5.3 Expenses page

Implement a genuine Craft Expenses page with:

* Period, category, treasury, supplier/party, and source filters.
* Pagination and search.
* Expense KPI summary.
* Manual Craft expense entry using canonical finance posting.
* Optional supplier/party association where valid.
* Receipt/document attachment only if the existing storage and document architecture can support it safely.
* Approval behavior only if a canonical approval model exists.
* Payment and treasury balance validation.
* Insufficient-balance handling.
* Journal entries.
* Audit records.
* Reversal instead of physical deletion of posted records.
* Genuine empty, loading, error, and permission states.

Do not invent a lifecycle that conflicts with existing finance tables or accounting rules. Reuse the closest established canonical model.

### 5.4 HPP and Profitability page

Implement an explainable Craft HPP and Profitability page based only on canonical data.

Use available sources such as:

* Product and variant prices.
* Product BOM.
* Product costing.
* Order items and recognized income.
* Production jobs.
* Actual material consumption.
* Material waste and failed prints.
* Marketplace fees and settlements.
* Other recorded production costs where the repository already has a canonical source.

Requirements:

* Period filtering.
* Product/order filters.
* Revenue.
* Recognized direct cost.
* Material cost.
* Waste/failure cost where measurable.
* Marketplace fee.
* Gross profit.
* Margin.
* Clear calculation basis or tooltip.
* Drill-down table where practical.
* No invented default electricity, labor, packaging, or overhead amounts.
* If a cost component is unavailable, show it as unavailable or excluded instead of silently guessing.
* Do not double-count order payments, invoice amounts, and marketplace settlements.
* Align calculations with Craft Analytics and the Global Report Center.

### 5.5 Cash Flow page

Implement:

* Cash in.
* Cash out.
* Net cash flow.
* Time-series breakdown.
* Breakdown by treasury account.
* Breakdown by category and source.
* Period, account, and currency filters.
* Correct treatment of transfers and reversals.
* Export only through existing authorized export conventions.
* No double-counting between internal transfers and external cash flow.

### 5.6 Budgets page

Implement a Craft-scoped budget page using the canonical budget tables and services.

Support, as allowed by the existing model:

* Budget list.
* Period.
* Category or cost-center association.
* Planned amount.
* Actual amount.
* Remaining amount.
* Utilization percentage.
* Status.
* Create/edit draft budget.
* Approval if the canonical budget lifecycle supports it.
* Permission-aware controls.
* Validation of overlapping or invalid periods.
* Correct Craft business-unit ownership.

Do not create a second budget implementation if Global Unified Finance already provides the canonical service.

### 5.7 Journals and Periods page

Implement:

* Journal list.
* Journal number, date, source, description, debit, credit, and balance state.
* Journal detail and journal lines.
* Chart-of-accounts labels.
* Search and date filtering.
* Financial-period list.
* Open/closed status.
* Period creation where authorized.
* Period close/reopen through the existing canonical management permission.
* Prevention of posting into a closed period.
* Balanced journal validation.
* No direct editing or deletion of posted journal entries.
* Permission-aware lifecycle controls.

If `finance.manage` is the canonical permission for period lifecycle, continue using it. Do not weaken the security model by assigning period management to ordinary Craft write access.

### 5.8 Craft Finance implementation layers

Complete every required layer:

* Database migration only if necessary.
* Schema snapshot consistency according to repository conventions.
* Backend schema validation.
* Repository/service logic.
* Controller and routes.
* Permission middleware.
* Audit logging.
* Domain event or notification hooks where canonical.
* Frontend API client.
* TypeScript types.
* React pages and components.
* Empty/loading/error states.
* Responsive layout.
* Automated backend smoke tests.
* Browser acceptance coverage.

## 6. Known gap 2 — Implement real global search

The search field in `Header.tsx` currently appears to be only a visual input with a keyboard-focus shortcut.

Implement an actual permission-aware global search.

### Search requirements

* Use a secure backend aggregator instead of unrestricted frontend calls to every module.
* Enforce organization and business-unit isolation.
* Enforce source-module read permissions for every result.
* A user must never receive result titles, counts, IDs, or metadata from a module they cannot read.
* Debounce requests.
* Require a sensible minimum query length.
* Apply a result limit and server-side pagination or category limits.
* Search at least the entities promised by the current placeholder:

  * Craft orders.
  * Studio projects.
  * Craft customers/partners.
  * Studio clients.
* Include products, Studio services, and documents only if their canonical services can be queried safely.
* Return normalized result types containing a safe internal route.
* Never trust a backend-provided arbitrary external navigation URL.
* Add keyboard navigation:

  * `Ctrl/Cmd+K` focuses or opens search.
  * Arrow keys navigate results.
  * Enter opens a selected result.
  * Escape closes the result panel.
* Add loading, no-results, error, and retry states.
* Close the result panel on route change and outside click.
* Make it responsive and accessible.
* Add tests proving that restricted users cannot discover unauthorized records.

If the repository already contains an unfinished search endpoint or component, complete it rather than creating a competing implementation.

## 7. Known gap 3 — Honor the user’s default workspace

The profile stores `default_workspace_code`, but the current workspace provider previously initialized Craft statically.

Implement correct default-workspace behavior.

Requirements:

* After authentication is loaded, initialize the active workspace from `user.default_workspace_code`.
* Avoid briefly rendering the wrong workspace while authentication is loading.
* On a fresh login or full page reload, route the user to the configured default workspace when entering a workspace-specific part of the application.
* Global routes must remain global and must not be unnecessarily redirected.
* A profile update to `default_workspace_code` must update/refetch the authenticated principal.
* The new default must reliably apply on the next fresh login/reload.
* Changing the profile default should not unexpectedly move the user away from their current unsaved page unless the current product behavior explicitly requires it.
* If a user cannot access their configured default business unit, fall back safely to the first accessible workspace.
* Do not create redirect loops.
* Keep the manual Craft/Studio workspace switch functional.
* Add authentication, reload, fallback, and navigation tests.

Inspect whether the authenticated principal needs to include an explicit accessible-workspaces list. Add it securely if necessary.

## 8. Known gap 4 — Fix Craft Orders frontend permission consistency

The Craft Orders route must use the same frontend permission protection pattern as the other modules.

Requirements:

* Wrap `/app/craft/orders/*` with `PermissionGate permission="craft.orders.read"` or the current canonical equivalent.
* Keep backend `craft.orders.read` and `craft.orders.write` authorization authoritative.
* Confirm the sidebar hides Craft Orders when the user lacks read access.
* Confirm direct URL access displays the standard access-denied state and does not render partial order data.
* Confirm write controls are hidden or disabled without `craft.orders.write`.
* Add direct-route read/write permission tests.

Do not rely only on failed API calls to protect the frontend page.

## 9. Known gap 5 — Add Craft Automation Event Inbox

The Craft Automation backend already exposes event data, but the Craft-specific UI previously had no Event Inbox route.

Implement:

* Sidebar child item: `Inbox Event`.
* Route: `/app/craft/automations/events`.
* A Craft Event Inbox page following the existing Studio or Global Automation design patterns.
* Craft business-unit scoping.
* `craft.automations.read` protection.
* Event name.
* Entity type/code.
* Source module.
* Status.
* Attempt count.
* Creation/processing time.
* Error information where safely available.
* Search/filter/pagination if supported by the current backend.
* Detail or safe navigation where useful.
* Loading, empty, error, and retry states.
* Browser acceptance coverage.

Reuse existing automation API clients and types where appropriate. Do not clone large components unnecessarily.

## 10. Known gap 6 — Make integration availability truthful and test-ready

Audit the Global Integrations and Craft Marketplace integration implementations.

Known provider categories include:

* Google Drive.
* Google Sheets.
* Google Calendar.
* WhatsApp Cloud API.
* Shopee.
* TikTok Shop.
* Tokopedia.
* Payment Gateway.
* Custom API / Generic Webhook.
* Development-only Mock Test Connector.

Requirements:

* A provider must be marked `available` only when a real connector exists.
* Planned providers must show a clear unavailable/planned state.
* Users must not be allowed to create, test, enable, or sync a provider that has no real connector.
* Mock Test Connector must never be exposed in production.
* Credentials must remain encrypted and must never be returned, logged, audited in plain text, or included in error responses.
* Test and sync actions must have proper timeouts, error handling, audit logs, and permission checks.
* The UI must clearly distinguish:

  * Available and configured.
  * Available but not configured.
  * Failed.
  * Disabled.
  * Planned/unavailable.
* Add tests for provider availability and production exclusion of the mock connector.
* Verify that Global Integrations and Craft Marketplace integrations do not maintain contradictory connection states.

Do not pretend that Shopee, TikTok Shop, Tokopedia, or a payment gateway is fully integrated without the official API implementation and required credentials.

Any item fundamentally blocked by third-party onboarding, credentials, API approval, merchant status, or external documentation may remain conditional. However:

* Its internal unavailable state must be correct.
* Its controls must be safe.
* Its required external prerequisite must be documented precisely.
* It must not be presented as completed.

### Custom API security

Do not enable Custom API / Generic Webhook unless comprehensive SSRF protections exist.

If implementing it, include at minimum:

* HTTPS/scheme allowlist.
* DNS validation.
* Blocking loopback, private, link-local, multicast, metadata, and reserved networks.
* Validation after DNS resolution.
* Validation on every redirect.
* Strict redirect limit.
* Strict connection and response timeout.
* Response-size limit.
* Safe content-type handling.
* No credential forwarding to redirected hosts.
* Audit logging without secret leakage.
* Tests for IPv4, IPv6, encoded addresses, redirects, and DNS rebinding scenarios.

If this hardening cannot be completed safely, keep the provider planned.

## 11. Known gap 7 — Route, sidebar, and control completeness

After fixing the known gaps, perform a second audit.

Ensure:

* Every sidebar path has a matching frontend route.
* Every child route renders the intended page.
* No known route falls through to `PlannedModulePage`.
* Unknown routes may still use the generic catch-all.
* No visible primary action button is a no-op.
* No menu item silently redirects to an unrelated module.
* All detail/edit/new routes are reachable.
* All frontend API calls have matching backend endpoints.
* All backend endpoints used by the UI are mounted.
* Every module has correct loading, empty, error, permission, and retry states.
* Empty databases do not crash dashboards or reports.
* No legacy sample data appears.
* No page claims that a provider, report, calculation, or workflow works when it does not.
* Indonesian labels and terminology remain consistent.

Create or update a route-coverage/browser script that navigates through every known sidebar route and verifies:

* The expected page heading.
* No `Dalam Pengembangan`.
* No unintended generic finance placeholder.
* No unexpected `Akses Ditolak` for an all-access user.
* No uncaught browser errors.
* No failed API request caused by missing endpoints.

## 12. Additional audit and remediation scope

If the repository contains other incomplete code-level functionality not listed above, repair it when it falls within the current product promises.

Examples include:

* A form that submits to a nonexistent endpoint.
* A button that has no action.
* A route that has no page.
* A page that displays mock figures.
* A backend endpoint that returns incorrect cross-workspace data.
* A role that can see a module but cannot load its required references.
* A generated document that is not registered in the Document Center.
* A deadline that is not projected into Calendar.
* An important write action that creates no audit entry.
* Financial data that fails to appear in Unified Finance or Analytics.
* A source event that never reaches the automation outbox.
* Incorrect empty-state behavior.
* Missing pagination, filters, or validation where existing comparable modules already provide them.

Do not expand the product into unrelated speculative features. Complete what the current routes, sidebar, API, schemas, and UI already promise.

## 13. Cross-module integrity requirements

All new or repaired functionality must preserve the following:

### Finance

* One canonical financial source of truth.
* Correct organization and business-unit ownership.
* No double-counting.
* Idempotent payment, settlement, transfer, and manual posting operations.
* Balanced journals.
* Closed-period enforcement.
* Treasury balance integrity.
* Correct handling of reversals.
* Historical records must not be physically deleted.

### Audit

Add meaningful audit records for:

* Manual income/expense.
* Reversal.
* Budget lifecycle.
* Period lifecycle.
* Integration credential lifecycle without revealing secrets.
* Integration test/sync/enable/disable.
* Any other repaired write operation.

### Documents

If repaired functionality produces a real document or receipt, register it in the canonical Document Center without duplicating source-owned files.

### Calendar and tasks

If a repaired feature contains a real operational deadline already covered by the calendar architecture, ensure its projection is deterministic and idempotent.

### Notifications and automation

Publish canonical domain events where existing architecture expects them. Do not directly duplicate notifications if the system sensor or automation engine already owns that responsibility.

### Settings

Use organization currency, timezone, week-start, and document branding settings rather than hard-coded assumptions.

### Security

* Validate every request with Zod or the repository’s canonical validation pattern.
* Protect every endpoint with authentication and explicit permissions.
* Enforce organization and business-unit scope in SQL.
* Validate file access and storage paths.
* Never expose integration secrets.
* Never leak unauthorized global-search results.
* Sanitize safe navigation targets.

## 14. Database and migration requirements

Before adding a migration, confirm whether the current schema already supports the feature.

If a migration is required:

* Make it additive and backward compatible.
* Use the next migration number.
* Make it safe for an empty transactional database and an existing populated database.
* Add indexes required by search, filters, and finance queries.
* Add foreign keys and uniqueness constraints where appropriate.
* Avoid destructive table recreation.
* Preserve historical records.
* Update schema snapshots/dumps only according to the repository’s established process.
* Do not add permanent sample records.
* Keep system reference data and roles consistent.
* Document the migration and rollback considerations.

Do not silently alter historical migrations that may already have been applied.

## 15. UI and UX requirements

* Preserve the current visual design.
* Reuse existing Card, Button, Modal, Badge, table, filter, empty-state, and confirmation components.
* Keep all user-facing application text in Indonesian.
* Support desktop, tablet, and mobile layouts.
* Avoid horizontal overflow except inside intentionally scrollable tables.
* Use confirmation dialogs for destructive or financially significant actions.
* Prevent duplicate submissions.
* Disable actions while requests are processing.
* Display meaningful server validation messages.
* Use consistent currency and date formatting.
* Use Asia/Jakarta or the configured organization timezone through canonical helpers.
* Add accessible labels and keyboard behavior.
* Hide unauthorized write actions rather than allowing them to fail after interaction.

## 16. Automated verification

Inspect the actual scripts in both root and server `package.json` files and run all relevant existing commands.

At minimum, verify:

* Frontend TypeScript/build.
* Backend TypeScript/build.
* Existing smoke tests.
* Existing browser acceptance scripts affected by the changes.
* Database migration compatibility.
* Empty-state behavior.
* Populated-state behavior using isolated fixtures.
* Permission denial.
* Organization/business-unit isolation.
* Finance arithmetic and idempotency.
* Global-search permission leakage.
* Workspace initialization.
* Route completeness.
* No uncaught browser errors.

Create or update tests for:

1. Craft Income.
2. Craft Expenses.
3. Craft Profitability.
4. Craft Cash Flow.
5. Craft Budgets.
6. Craft Journals and Periods.
7. Global Search.
8. Default Workspace.
9. Craft Orders direct-route permissions.
10. Craft Automation Event Inbox.
11. Integration provider availability.
12. Sidebar route coverage.

If a browser test does not exist for Craft Finance, create one following the repository’s current browser acceptance conventions.

Tests must create their own uniquely identifiable fixtures and clean them safely. Never rely on existing business records.

## 17. Required acceptance criteria

The task is complete only when all of the following are true:

* The six Craft Finance routes no longer use `FinancePlaceholderPage`.
* Their data comes from real canonical backend services.
* Finance writes create correct treasury, transaction, journal, and audit effects.
* HPP calculations are explainable and do not guess unavailable costs.
* Global search returns real, permission-safe results.
* `default_workspace_code` is honored after login/reload.
* Craft Orders has a frontend read-permission gate.
* Craft Automation has an Event Inbox page and sidebar route.
* Planned integration providers cannot be falsely configured or synchronized.
* Every sidebar route is reachable and intentional.
* No known route renders `Dalam Pengembangan`.
* An empty transactional database is handled cleanly.
* No sample or legacy mock data appears.
* All relevant builds and tests pass.
* Any third-party-dependent limitation is documented honestly and precisely.

## 18. Working method

Proceed in this order:

1. Inspect `git status` and preserve unrelated changes.
2. Audit the current repository and map the gaps.
3. Inspect existing finance, automation, integration, permission, and database conventions.
4. Implement shared backend/canonical changes first.
5. Implement frontend API and types.
6. Implement the UI pages.
7. Add migrations only when necessary.
8. Add or update automated tests.
9. Run focused tests while iterating.
10. Run the broadest safe build and test suite.
11. Perform a final route and placeholder search.
12. Deliver a precise completion report.

Do not pause after the audit unless a genuine external or permission blocker prevents safe implementation.

When an external dependency is blocked, continue implementing all unrelated repository-level work.

## 19. Final response format

At completion, report:

### A. Completion summary

A concise statement of what is now finished.

### B. Resolved-gap matrix

For every known and newly discovered gap, provide:

* Gap.
* Previous state.
* Implemented solution.
* Status: resolved, conditionally resolved, or externally blocked.

### C. Files changed

Group by:

* Frontend.
* Backend.
* Database/migrations.
* Tests.
* Documentation.

### D. Database impact

Explain:

* Tables used or changed.
* Migration added.
* Indexes/constraints.
* Compatibility with empty and populated databases.

### E. Security and permission impact

List every permission and isolation rule affected.

### F. Verification results

Provide the exact commands run and whether each passed.

Do not claim a test passed if it was not executed.

### G. Remaining external prerequisites

List only genuine external blockers such as marketplace credentials, API approvals, payment-gateway onboarding, or cloud-service credentials.

### H. Manual follow-up

List any safe manual configuration steps required before the formal UAT.

Do not create the formal manual UAT Spreadsheet in this task. The next task will use the completed implementation to construct the systematic test-case Spreadsheet.
