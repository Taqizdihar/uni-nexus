import React, { useEffect, useMemo, useState } from 'react';
import { ArrowLeft, CalendarDays, ClipboardList, FileText, Package, Plus, Save, ShoppingCart, Trash2, Truck, UserPlus, X } from 'lucide-react';
import { useNavigate } from 'react-router-dom';
import { Card } from '../../../components/ui/Card';
import { Button } from '../../../components/ui/Button';
import { craftOrdersApi } from '../../../services/api/craft-orders.api';
import { formatCurrency } from '../../../lib/utils';
import type { CreateCraftOrderRequest, CustomerOption, ProductOption, SalesChannelOption } from '../../../types/craft-orders';
import { FormField, OrderPageHeader, OrderSectionHeader, SegmentedControl } from './components/OrdersUI';

type ItemMode = 'catalog' | 'custom';
interface DraftItem { mode: ItemMode; product_id: string; variant_id: string; item_name: string; item_description: string; quantity: number; unit_price: number; discount_amount: number; estimated_material_g: number | ''; estimated_print_minutes: number | ''; material: string; color: string; size: string; specification: string; }
const emptyItem = (): DraftItem => ({ mode: 'custom', product_id: '', variant_id: '', item_name: '', item_description: '', quantity: 1, unit_price: 0, discount_amount: 0, estimated_material_g: '', estimated_print_minutes: '', material: '', color: '', size: '', specification: '' });
const moneyInput = 'orders-input';

export function OrderCreatePage() {
  const navigate = useNavigate();
  const [customers, setCustomers] = useState<CustomerOption[]>([]); const [channels, setChannels] = useState<SalesChannelOption[]>([]); const [products, setProducts] = useState<ProductOption[]>([]); const [items, setItems] = useState<DraftItem[]>([emptyItem()]);
  const [saving, setSaving] = useState(false); const [error, setError] = useState<string | null>(null); const [notice, setNotice] = useState<string | null>(null); const [showCustomerModal, setShowCustomerModal] = useState(false); const [customerSearch, setCustomerSearch] = useState(''); const [newCustomer, setNewCustomer] = useState({ display_name: '', party_kind: 'individual' as CustomerOption['party_kind'], email: '', phone: '' }); const [creatingCustomer, setCreatingCustomer] = useState(false);
  const [form, setForm] = useState({ customer_party_id: '', sales_channel_id: '', external_order_id: '', order_type: 'standard' as CreateCraftOrderRequest['order_type'], deadline_at: '', priority_mode: 'automatic' as 'automatic' | 'manual', priority_code: 'normal', priority_reason: '', discount_amount: 0, shipping_amount: 0, marketplace_fee_amount: 0, tax_amount: 0, customer_notes: '', internal_notes: '', shipping_recipient_name: '', shipping_phone: '', shipping_address: '', courier_name: '' });

  useEffect(() => { void (async () => { try { const [customerRows, channelRows, productRows] = await Promise.all([craftOrdersApi.getCustomers(), craftOrdersApi.getSalesChannels(), craftOrdersApi.getProducts()]); setCustomers(customerRows); setChannels(channelRows); setProducts(productRows); } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Gagal memuat data referensi pesanan.'); } })(); }, []);
  useEffect(() => { const timer = window.setTimeout(() => { if (customerSearch.trim()) void craftOrdersApi.getCustomers(customerSearch).then(setCustomers).catch(() => undefined); }, 250); return () => window.clearTimeout(timer); }, [customerSearch]);
  const subtotal = useMemo(() => items.reduce((total, item) => total + (Number(item.quantity) * Number(item.unit_price)) - Number(item.discount_amount), 0), [items]);
  const total = subtotal - form.discount_amount + form.shipping_amount + form.tax_amount;
  const selectedChannel = channels.find(channel => String(channel.id) === form.sales_channel_id);
  const setItem = (index: number, changes: Partial<DraftItem>) => setItems(current => current.map((item, itemIndex) => itemIndex === index ? { ...item, ...changes } : item));
  const selectProduct = (index: number, productId: string) => { const product = products.find(row => row.id === Number(productId)); if (!product) return; setItem(index, { mode: 'catalog', product_id: productId, variant_id: '', item_name: product.name, unit_price: product.base_selling_price, estimated_material_g: product.estimated_weight_g ?? '', estimated_print_minutes: product.estimated_print_minutes ?? '', item_description: product.sku }); };
  const selectVariant = (index: number, variantId: string) => { const product = products.find(row => row.id === Number(items[index].product_id)); const variant = product?.variants.find(row => row.id === Number(variantId)); if (!product || !variant) return; setItem(index, { variant_id: variantId, item_name: `${product.name} — ${variant.name}`, unit_price: variant.selling_price, estimated_material_g: variant.estimated_weight_g ?? product.estimated_weight_g ?? '', estimated_print_minutes: variant.estimated_print_minutes ?? product.estimated_print_minutes ?? '' }); };
  const createCustomer = async (event: React.FormEvent) => { event.preventDefault(); setCreatingCustomer(true); setError(null); try { const customer = await craftOrdersApi.quickCreateCustomer({ ...newCustomer, email: newCustomer.email || null, phone: newCustomer.phone || null }); setCustomers(current => [...current, customer].sort((a, b) => a.display_name.localeCompare(b.display_name))); setForm(current => ({ ...current, customer_party_id: String(customer.id) })); setShowCustomerModal(false); setNewCustomer({ display_name: '', party_kind: 'individual', email: '', phone: '' }); setNotice('Pelanggan baru dibuat dan telah dipilih.'); } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Gagal membuat pelanggan.'); } finally { setCreatingCustomer(false); } };
  const submit = async (event: React.FormEvent) => { event.preventDefault(); setError(null); setNotice(null); if (!form.customer_party_id || !form.sales_channel_id) { setError('Pelanggan dan kanal penjualan wajib dipilih.'); return; } if (items.some(item => !item.item_name.trim() || item.quantity <= 0 || item.unit_price < 0)) { setError('Lengkapi nama, jumlah, dan harga untuk setiap item.'); return; } setSaving(true); try { const payload: CreateCraftOrderRequest = { customer_party_id: Number(form.customer_party_id), sales_channel_id: Number(form.sales_channel_id), external_order_id: form.external_order_id || null, order_type: form.order_type, deadline_at: form.deadline_at || null, is_priority_manual: form.priority_mode === 'manual', priority_code: form.priority_mode === 'manual' ? form.priority_code as CreateCraftOrderRequest['priority_code'] : 'normal', priority_reason: form.priority_mode === 'manual' ? form.priority_reason || null : null, discount_amount: Number(form.discount_amount), shipping_amount: Number(form.shipping_amount), marketplace_fee_amount: Number(form.marketplace_fee_amount), tax_amount: Number(form.tax_amount), customer_notes: form.customer_notes || null, internal_notes: form.internal_notes || null, shipping_recipient_name: form.shipping_recipient_name || null, shipping_phone: form.shipping_phone || null, shipping_address: form.shipping_address || null, courier_name: form.courier_name || null, items: items.map(item => ({ product_id: item.mode === 'catalog' && item.product_id ? Number(item.product_id) : null, variant_id: item.mode === 'catalog' && item.variant_id ? Number(item.variant_id) : null, item_name: item.item_name.trim(), item_description: item.item_description || null, quantity: Number(item.quantity), unit_price: Number(item.unit_price), discount_amount: Number(item.discount_amount), estimated_material_g: item.estimated_material_g === '' ? null : Number(item.estimated_material_g), estimated_print_minutes: item.estimated_print_minutes === '' ? null : Number(item.estimated_print_minutes), custom_spec_json: item.mode === 'custom' ? { material: item.material, color: item.color, size: item.size, specification: item.specification } : null })) }; const result = await craftOrdersApi.createOrder(payload); navigate(`/app/craft/orders/${result.id}`); } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Gagal membuat pesanan.'); } finally { setSaving(false); } };
  const setFormValue = <K extends keyof typeof form>(key: K, value: (typeof form)[K]) => setForm(current => ({ ...current, [key]: value }));

  return <div className="space-y-6 pb-12">
    <OrderPageHeader title="Pesanan Baru" description="Buat pesanan Craft dari katalog atau spesifikasi custom." back={() => navigate('/app/craft/orders')} />
    {error && <div className="rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{error}</div>}{notice && <div className="rounded-lg border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-700">{notice}</div>}
    <form onSubmit={submit} className="grid items-start gap-6 xl:grid-cols-[minmax(0,1fr)_330px]">
      <div className="space-y-5">
        <Card><div className="space-y-5 p-5 sm:p-6"><OrderSectionHeader number="01" icon={ClipboardList} title="Pelanggan & Pesanan" description="Informasi sumber dan kebutuhan pesanan." /><div className="grid grid-cols-1 gap-4 md:grid-cols-2">
          <FormField label="Cari pelanggan"><div className="relative"><SearchIcon /><input className={`${moneyInput} pl-10`} value={customerSearch} onChange={event => setCustomerSearch(event.target.value)} placeholder="Nama atau nomor telepon..." /></div></FormField>
          <FormField label="Pilih pelanggan" required><div className="flex gap-2"><select className={`${moneyInput} min-w-0 flex-1`} value={form.customer_party_id} onChange={event => setFormValue('customer_party_id', event.target.value)}><option value="">Pilih pelanggan</option>{customers.map(customer => <option key={customer.id} value={customer.id}>{customer.display_name}{customer.phone ? ` — ${customer.phone}` : ''}</option>)}</select><Button type="button" variant="outline" className="shrink-0 px-3" onClick={() => setShowCustomerModal(true)}><UserPlus className="h-4 w-4" /><span className="hidden sm:inline">Pelanggan</span></Button></div></FormField>
          <FormField label="Kanal penjualan" required hint={selectedChannel?.channel_type ? `Tipe kanal: ${selectedChannel.channel_type}` : undefined}><select className={moneyInput} value={form.sales_channel_id} onChange={event => setFormValue('sales_channel_id', event.target.value)}><option value="">Pilih kanal</option>{channels.map(channel => <option key={channel.id} value={channel.id}>{channel.name}</option>)}</select></FormField>
          <FormField label="ID pesanan marketplace" hint={selectedChannel?.channel_type === 'marketplace' ? 'Terhubung ke pesanan eksternal.' : 'Opsional untuk kanal non-marketplace.'}><input className={moneyInput} value={form.external_order_id} onChange={event => setFormValue('external_order_id', event.target.value)} placeholder="Contoh: SP-12345" /></FormField>
          <FormField label="Tipe pesanan"><select className={moneyInput} value={form.order_type} onChange={event => setFormValue('order_type', event.target.value as CreateCraftOrderRequest['order_type'])}><option value="standard">Standar</option><option value="custom">Custom</option><option value="partner">Mitra</option><option value="internal">Internal</option></select></FormField>
          <FormField label="Tenggat waktu"><span className="relative"><CalendarDays className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-[var(--nexus-muted)]" /><input type="datetime-local" className={`${moneyInput} pl-10`} value={form.deadline_at} onChange={event => setFormValue('deadline_at', event.target.value)} /></span></FormField>
        </div><div className="border-t border-[var(--nexus-border)] pt-5"><SegmentedControl label="Mode prioritas" value={form.priority_mode} options={[{ value: 'automatic', label: 'Otomatis' }, { value: 'manual', label: 'Manual' }]} onChange={value => setFormValue('priority_mode', value)} />{form.priority_mode === 'automatic' ? <p className="mt-2 text-xs text-[var(--nexus-muted)]">Sistem menentukan prioritas berdasarkan tenggat dan estimasi produksi.</p> : <div className="mt-4 grid gap-4 md:grid-cols-2"><FormField label="Prioritas"><select className={moneyInput} value={form.priority_code} onChange={event => setFormValue('priority_code', event.target.value)}><option value="low">Rendah</option><option value="normal">Normal</option><option value="high">Tinggi</option><option value="critical">Kritis</option></select></FormField><FormField label="Alasan prioritas"><input className={moneyInput} value={form.priority_reason} onChange={event => setFormValue('priority_reason', event.target.value)} placeholder="Opsional" /></FormField></div>}</div></div></Card>
        <Card><div className="space-y-5 p-5 sm:p-6"><OrderSectionHeader number="02" icon={ShoppingCart} title="Item Pesanan" description="Pisahkan setiap item agar detail produksi dan biaya mudah dilacak." action={<Button type="button" size="sm" variant="outline" onClick={() => setItems(current => [...current, emptyItem()])}><Plus className="h-4 w-4" /> Tambah Item</Button>} />{products.length === 0 && <p className="rounded-lg border border-dashed border-[var(--nexus-border)] bg-[var(--nexus-cream-soft)] p-3 text-xs text-[var(--nexus-muted)]">Belum ada produk di katalog. Anda tetap dapat membuat item Custom.</p>}{items.map((item, index) => <ItemCard key={index} item={item} index={index} products={products} itemsLength={items.length} setItem={setItem} selectProduct={selectProduct} selectVariant={selectVariant} remove={() => setItems(current => current.filter((_item, itemIndex) => itemIndex !== index))} />)}</div></Card>
        <Card><div className="space-y-5 p-5 sm:p-6"><OrderSectionHeader number="03" icon={Truck} title="Pengiriman & Catatan" description="Simpan informasi penerima, instruksi, dan biaya tambahan." /><div className="grid grid-cols-2 gap-4"><FormField label="Diskon pesanan"><MoneyInput value={form.discount_amount} onChange={value => setFormValue('discount_amount', value)} /></FormField><FormField label="Ongkos kirim"><MoneyInput value={form.shipping_amount} onChange={value => setFormValue('shipping_amount', value)} /></FormField><FormField label="Biaya marketplace" hint="Biaya internal, tidak ditagihkan."><MoneyInput value={form.marketplace_fee_amount} onChange={value => setFormValue('marketplace_fee_amount', value)} /></FormField><FormField label="Pajak"><MoneyInput value={form.tax_amount} onChange={value => setFormValue('tax_amount', value)} /></FormField></div><div className="grid gap-4 md:grid-cols-2"><FormField label="Catatan pelanggan"><textarea className="orders-textarea" value={form.customer_notes} onChange={event => setFormValue('customer_notes', event.target.value)} placeholder="Instruksi yang terlihat pelanggan..." /></FormField><FormField label="Catatan internal"><textarea className="orders-textarea" value={form.internal_notes} onChange={event => setFormValue('internal_notes', event.target.value)} placeholder="Catatan untuk tim internal..." /></FormField><FormField label="Nama penerima"><input className={moneyInput} value={form.shipping_recipient_name} onChange={event => setFormValue('shipping_recipient_name', event.target.value)} /></FormField><FormField label="Telepon penerima"><input className={moneyInput} value={form.shipping_phone} onChange={event => setFormValue('shipping_phone', event.target.value)} /></FormField><FormField label="Kurir"><input className={moneyInput} value={form.courier_name} onChange={event => setFormValue('courier_name', event.target.value)} /></FormField><FormField label="Alamat pengiriman"><textarea className="orders-textarea" value={form.shipping_address} onChange={event => setFormValue('shipping_address', event.target.value)} /></FormField></div></div></Card>
      </div>
      <aside className="xl:sticky xl:top-6"><Card className="overflow-hidden"><div className="border-b border-[var(--nexus-border)] bg-[var(--nexus-charcoal)] p-5 text-white"><p className="text-[10px] font-bold uppercase tracking-[0.18em] text-[var(--nexus-yellow)]">Ringkasan pesanan</p><div className="mt-2 flex items-end justify-between gap-3"><span className="text-sm text-white/70">{items.length} item</span><span className="text-2xl font-bold tracking-tight">{formatCurrency(total)}</span></div></div><div className="space-y-3 p-5 text-sm"><SummaryRow label="Subtotal" value={formatCurrency(subtotal)} /><SummaryRow label="Diskon" value={`-${formatCurrency(form.discount_amount)}`} negative={form.discount_amount > 0} /><SummaryRow label="Ongkos kirim" value={formatCurrency(form.shipping_amount)} /><SummaryRow label="Pajak" value={formatCurrency(form.tax_amount)} /><div className="my-4 border-t border-[var(--nexus-border)]" /><div className="flex items-end justify-between gap-3"><span className="font-bold text-[var(--nexus-charcoal)]">Total pelanggan</span><span className="text-lg font-bold text-[var(--nexus-charcoal)]">{formatCurrency(total)}</span></div><div className="mt-4 rounded-lg bg-[var(--nexus-cream-soft)] p-3"><div className="flex justify-between gap-3 text-xs"><span className="text-[var(--nexus-muted)]">Biaya marketplace</span><span className="font-semibold text-[var(--nexus-charcoal)]">{formatCurrency(form.marketplace_fee_amount)}</span></div><p className="mt-1 text-[10px] leading-4 text-[var(--nexus-muted)]">Biaya internal — tidak ditagihkan ke pelanggan.</p></div><Button type="submit" className="mt-3 h-11 w-full" disabled={saving}><Save className="h-4 w-4" />{saving ? 'Menyimpan...' : 'Simpan Pesanan'}</Button><p className="text-center text-[10px] leading-4 text-[var(--nexus-muted)]">Periksa kembali detail sebelum menyimpan.</p></div></Card></aside>
    </form>
    {showCustomerModal && <div className="fixed inset-0 z-50 flex items-center justify-center bg-[var(--nexus-charcoal)]/45 p-4 backdrop-blur-[2px]"><form onSubmit={createCustomer} className="w-full max-w-md space-y-5 rounded-xl border border-[var(--nexus-border)] bg-white p-6 shadow-2xl"><div className="flex items-start justify-between"><div><p className="orders-eyebrow">Customer master</p><h2 className="text-lg font-bold text-[var(--nexus-charcoal)]">Tambah Pelanggan</h2></div><button type="button" className="rounded-lg p-2 text-[var(--nexus-muted)] hover:bg-[var(--nexus-cream-soft)]" onClick={() => setShowCustomerModal(false)} aria-label="Tutup"><X className="h-4 w-4" /></button></div><div className="space-y-4"><FormField label="Nama" required><input required className={moneyInput} value={newCustomer.display_name} onChange={event => setNewCustomer(current => ({ ...current, display_name: event.target.value }))} /></FormField><FormField label="Jenis"><select className={moneyInput} value={newCustomer.party_kind} onChange={event => setNewCustomer(current => ({ ...current, party_kind: event.target.value as CustomerOption['party_kind'] }))}><option value="individual">Individual</option><option value="company">Perusahaan</option><option value="institution">Institusi</option></select></FormField><div className="grid grid-cols-2 gap-4"><FormField label="Email"><input type="email" className={moneyInput} value={newCustomer.email} onChange={event => setNewCustomer(current => ({ ...current, email: event.target.value }))} /></FormField><FormField label="Nomor telepon"><input className={moneyInput} value={newCustomer.phone} onChange={event => setNewCustomer(current => ({ ...current, phone: event.target.value }))} /></FormField></div></div><div className="flex justify-end gap-2 border-t border-[var(--nexus-border)] pt-4"><Button type="button" variant="outline" onClick={() => setShowCustomerModal(false)}>Batal</Button><Button type="submit" disabled={creatingCustomer}>{creatingCustomer ? 'Menyimpan...' : 'Simpan Pelanggan'}</Button></div></form></div>}
  </div>;
}

function SearchIcon() { return <svg className="absolute left-3.5 top-1/2 h-4 w-4 -translate-y-1/2 text-[var(--nexus-muted)]" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2"><circle cx="11" cy="11" r="8" /><path d="m21 21-4.3-4.3" /></svg>; }
function MoneyInput({ value, onChange }: { value: number; onChange: (value: number) => void }) { return <div className="relative"><span className="absolute left-3 top-1/2 -translate-y-1/2 text-xs font-semibold text-[var(--nexus-muted)]">Rp</span><input min="0" type="number" className="orders-input pl-9" value={value} onChange={event => onChange(Number(event.target.value))} /></div>; }
function SummaryRow({ label, value, negative }: { label: string; value: string; negative?: boolean }) { return <div className="flex justify-between gap-3"><span className="text-[var(--nexus-muted)]">{label}</span><span className={negative ? 'text-red-600' : 'font-medium text-[var(--nexus-charcoal)]'}>{value}</span></div>; }
function ItemCard({ item, index, products, itemsLength, setItem, selectProduct, selectVariant, remove }: { item: DraftItem; index: number; products: ProductOption[]; itemsLength: number; setItem: (index: number, changes: Partial<DraftItem>) => void; selectProduct: (index: number, productId: string) => void; selectVariant: (index: number, variantId: string) => void; remove: () => void; key?: React.Key }) {
  const lineTotal = (Number(item.quantity) * Number(item.unit_price)) - Number(item.discount_amount);
  const product = products.find(row => row.id === Number(item.product_id));
  return <div className="rounded-xl border border-[var(--nexus-border)] bg-[var(--nexus-cream-soft)]/35 p-4 sm:p-5"><div className="flex items-start justify-between gap-4"><div><div className="flex items-center gap-2"><span className="text-[10px] font-bold uppercase tracking-[0.15em] text-[var(--nexus-muted)]">Item #{index + 1}</span><span className="rounded-full bg-[var(--nexus-charcoal)] px-2 py-1 text-[9px] font-bold uppercase tracking-wider text-[var(--nexus-yellow)]">{item.mode === 'custom' ? 'Custom' : 'Katalog'}</span></div><p className="mt-1 font-semibold text-[var(--nexus-charcoal)]">{item.item_name || 'Item baru'}</p></div>{itemsLength > 1 && <Button type="button" size="sm" variant="ghost" className="text-red-600 hover:bg-red-50" onClick={remove} aria-label={`Hapus item ${index + 1}`}><Trash2 className="h-4 w-4" /></Button>}</div><div className="mt-4"><SegmentedControl value={item.mode} options={[{ value: 'catalog', label: 'Katalog' }, { value: 'custom', label: 'Custom' }]} onChange={value => setItem(index, { ...emptyItem(), mode: value })} /></div>{item.mode === 'catalog' ? <div className="mt-4 space-y-4"><div className="grid gap-4 md:grid-cols-2"><FormField label="Produk" required><select className="orders-input" value={item.product_id} onChange={event => selectProduct(index, event.target.value)}><option value="">Pilih produk</option>{products.map(productOption => <option key={productOption.id} value={productOption.id}>{productOption.name} ({productOption.sku})</option>)}</select></FormField><FormField label="Varian"><select className="orders-input" value={item.variant_id} onChange={event => selectVariant(index, event.target.value)} disabled={!item.product_id}><option value="">Produk dasar</option>{product?.variants.map(variant => <option key={variant.id} value={variant.id}>{variant.name}</option>)}</select></FormField></div>{product && <div className="grid grid-cols-3 gap-2 rounded-lg border border-[var(--nexus-border)] bg-white p-3 text-xs"><InfoStat label="SKU" value={product.sku} /><InfoStat label="Harga dasar" value={formatCurrency(item.unit_price)} /><InfoStat label="Est. material" value={item.estimated_material_g === '' ? '-' : `${item.estimated_material_g} g`} /></div>}</div> : <div className="mt-4 grid gap-4 md:grid-cols-2"><FormField label="Nama item" required><input required className="orders-input" value={item.item_name} onChange={event => setItem(index, { item_name: event.target.value })} /></FormField><FormField label="Deskripsi"><textarea className="orders-textarea" value={item.item_description} onChange={event => setItem(index, { item_description: event.target.value })} /></FormField><FormField label="Material"><input className="orders-input" value={item.material} onChange={event => setItem(index, { material: event.target.value })} /></FormField><div className="grid grid-cols-2 gap-3"><FormField label="Warna"><input className="orders-input" value={item.color} onChange={event => setItem(index, { color: event.target.value })} /></FormField><FormField label="Ukuran"><input className="orders-input" value={item.size} onChange={event => setItem(index, { size: event.target.value })} /></FormField></div><FormField label="Spesifikasi custom" className="md:col-span-2"><textarea className="orders-textarea" value={item.specification} onChange={event => setItem(index, { specification: event.target.value })} /></FormField></div>}<div className="mt-4 grid grid-cols-2 gap-4 md:grid-cols-5"><FormField label="Jumlah" required><input required min="0.0001" step="0.0001" type="number" className="orders-input" value={item.quantity} onChange={event => setItem(index, { quantity: Number(event.target.value) })} /></FormField><FormField label="Harga satuan" required><MoneyInput value={item.unit_price} onChange={value => setItem(index, { unit_price: value })} /></FormField><FormField label="Diskon item"><MoneyInput value={item.discount_amount} onChange={value => setItem(index, { discount_amount: value })} /></FormField><FormField label="Est. material"><input min="0" type="number" className="orders-input" placeholder="gram" value={item.estimated_material_g} onChange={event => setItem(index, { estimated_material_g: event.target.value === '' ? '' : Number(event.target.value) })} /></FormField><FormField label="Est. cetak"><input min="0" type="number" className="orders-input" placeholder="menit" value={item.estimated_print_minutes} onChange={event => setItem(index, { estimated_print_minutes: event.target.value === '' ? '' : Number(event.target.value) })} /></FormField></div><div className="mt-4 flex items-center justify-between border-t border-[var(--nexus-border)] pt-3 text-xs"><span className="text-[var(--nexus-muted)]">Qty {item.quantity} × {formatCurrency(item.unit_price)}</span><span className="font-bold text-[var(--nexus-charcoal)]">Total item: {formatCurrency(lineTotal)}</span></div></div>;
}
function InfoStat({ label, value }: { label: string; value: string }) { return <div><p className="text-[10px] uppercase tracking-wider text-[var(--nexus-muted)]">{label}</p><p className="mt-1 truncate font-semibold text-[var(--nexus-charcoal)]">{value}</p></div>; }
