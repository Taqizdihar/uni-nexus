# Runtime variables are loaded from apps/api/.env and apps/admin/.env.
# Run npm run env:init to create local files without overwriting existing ones.
# See those folders' .env.example files for complete options.
