// Compatibility wrapper for existing Notifications imports. Shared modules use
// the generic utility so all Jakarta calendar boundaries stay identical.
export { jakartaBusinessDate, jakartaDayBoundsUtc, jakartaDayStartUtc, utcDateTimeSql, type DateInput } from '../time/jakarta-time';
