Error
Static analysis:

33 errors were found during analysis.

Unrecognized statement type. (near "DECLARE" at position 150)
Unrecognized statement type. (near "DECLARE" at position 184)
Unrecognized statement type. (near "DECLARE" at position 225)
Unrecognized statement type. (near "DECLARE" at position 268)
Unrecognized statement type. (near "DECLARE" at position 317)
Unrecognized statement type. (near "DECLARE" at position 363)
Unrecognized statement type. (near "DECLARE" at position 402)
Unrecognized statement type. (near "DECLARE" at position 938)
Unrecognized statement type. (near "DECLARE" at position 1009)
Unrecognized statement type. (near "RESIGNAL" at position 1186)
Unrecognized statement type. (near "END" at position 1201)
Unrecognized statement type. (near "IF" at position 1762)
Unrecognized statement type. (near "SIGNAL" at position 1971)
Unrecognized statement type. (near "END" at position 2043)
Unrecognized statement type. (near "IF" at position 2689)
Unrecognized statement type. (near "SIGNAL" at position 2883)
Unrecognized statement type. (near "END" at position 2955)
Unrecognized statement type. (near "IF" at position 3921)
Unrecognized statement type. (near "LEAVE" at position 4429)
Unrecognized statement type. (near "END" at position 4454)
Unrecognized statement type. (near "OPEN" at position 4729)
Unexpected beginning of statement. (near "reset_loop:" at position 4755)
Unrecognized statement type. (near "LOOP" at position 4767)
Unrecognized statement type. (near "IF" at position 4827)
Unrecognized statement type. (near "END" at position 4886)
Unrecognized statement type. (near "IF" at position 6015)
Unrecognized statement type. (near "SIGNAL" at position 6272)
Unrecognized statement type. (near "END" at position 6354)
Unrecognized statement type. (near "IF" at position 6827)
Unrecognized statement type. (near "END" at position 7185)
Unrecognized statement type. (near "END" at position 7793)
Unrecognized statement type. (near "CLOSE" at position 7810)
Unrecognized statement type. (near "END" at position 8148)

CREATE PROCEDURE `__uni_nexus_strict_test_reset`(    IN p_confirmation VARCHAR(100))reset_main: BEGIN    DECLARE v_done INT DEFAULT 0;    DECLARE v_table VARCHAR(64);    DECLARE v_deleted BIGINT DEFAULT 0;    DECLARE v_remaining BIGINT DEFAULT 0;    DECLARE v_has_auto_increment INT DEFAULT 0;    DECLARE v_required_tables INT DEFAULT 0;    DECLARE v_message VARCHAR(512);    DECLARE reset_cursor CURSOR FOR        SELECT t.`TABLE_NAME`        FROM `information_schema`.`TABLES` t        WHERE t.`TABLE_SCHEMA` = DATABASE()          AND t.`TABLE_TYPE` = 'BASE TABLE'          AND t.`TABLE_NAME` NOT IN (              'organizations',              'business_units',              'users',              'roles',              'permissions',              'role_permissions',              'user_roles',              'user_business_units'          )        ORDER BY t.`TABLE_NAME`;    DECLARE CONTINUE HANDLER FOR NOT FOUND        SET v_done = 1;    DECLARE EXIT HANDLER FOR SQLEXCEPTION    BEGIN        SET FOREIGN_KEY_CHECKS = @__reset_old_fk_checks;        SET SQL_SAFE_UPDATES   = @__reset_old_safe_updates;        RESIGNAL;    END;    -- ------------------------------------------------------------------------    -- Preserve current session settings.    -- ------------------------------------------------------------------------    SET @__reset_old_fk_checks    := @@SESSION.FOREIGN_KEY_CHECKS;    SET @__reset_old_safe_updates := @@SESSION.SQL_SAFE_UPDATES;    -- ------------------------------------------------------------------------    -- Absolute database-name guard.    -- ------------------------------------------------------------------------    IF DATABASE() <> 'uni-nexus' THEN        SET v_message = CONCAT(            'ABORTED: expected database uni-nexus, current database is ',            COALESCE(DATABASE(), '<NULL>')        );        SIGNAL SQLSTATE '45000'            SET MESSAGE_TEXT = v_message;    END IF;    -- ------------------------------------------------------------------------    -- Verify all protected tables exist.    -- ------------------------------------------------------------------------    SELECT COUNT(*)      INTO v_required_tables    FROM `information_schema`.`TABLES`    WHERE `TABLE_SCHEMA` = DATABASE()      AND `TABLE_TYPE` = 'BASE TABLE'      AND `TABLE_NAME` IN (          'organizations',          'business_units',          'users',          'roles',          'permissions',          'role_permissions',          'user_roles',          'user_business_units'      );    IF v_required_tables <> 8 THEN        SET v_message = CONCAT(            'ABORTED: expected 8 protected authentication tables, found ',            v_required_tables        );        SIGNAL SQLSTATE '45000'            SET MESSAGE_TEXT = v_message;    END IF;    -- ------------------------------------------------------------------------    -- Results remain available for this SQL session after procedure returns.    -- ------------------------------------------------------------------------    DROP TEMPORARY TABLE IF EXISTS `__uni_nexus_reset_results`;    CREATE TEMPORARY TABLE `__uni_nexus_reset_results` (        `table_name` VARCHAR(64) NOT NULL,        `rows_deleted` BIGINT NOT NULL DEFAULT 0,        `remaining_rows` BIGINT NOT NULL DEFAULT 0,        `auto_increment_reset` TINYINT(1) NOT NULL DEFAULT 0,        `note` VARCHAR(255) DEFAULT NULL,        PRIMARY KEY (`table_name`)    );    -- ------------------------------------------------------------------------    -- Confirmation guard.    --    -- Default script execution reaches here and STOPS WITHOUT DELETING DATA.    -- ------------------------------------------------------------------------    IF p_confirmation <> 'RESET-UNI-NEXUS-TEST-DATA' THEN        INSERT INTO `__uni_nexus_reset_results`        (            `table_name`,            `rows_deleted`,            `remaining_rows`,            `auto_increment_reset`,            `note`        )        VALUES        (            '<DRY-RUN>',            0,            0,            0,            'NO DATA DELETED. Set @CONFIRM_RESET = RESET-UNI-NEXUS-TEST-DATA only after reviewing the preview.'        );        LEAVE reset_main;    END IF;    -- ------------------------------------------------------------------------    -- DESTRUCTIVE SECTION    -- ------------------------------------------------------------------------    SET FOREIGN_KEY_CHECKS = 0;    SET SQL_SAFE_UPDATES = 0;    OPEN reset_cursor;    reset_loop: LOOP        FETCH reset_cursor INTO v_table;        IF v_done = 1 THEN            LEAVE reset_loop;        END IF;        -- --------------------------------------------------------------------        -- Delete every row.        -- --------------------------------------------------------------------        SET @__reset_sql = CONCAT(            'DELETE FROM `',            REPLACE(v_table, '`', '``'),            '`'        );        PREPARE __reset_stmt FROM @__reset_sql;        EXECUTE __reset_stmt;        SET v_deleted = ROW_COUNT();        DEALLOCATE PREPARE __reset_stmt;        -- --------------------------------------------------------------------        -- Exact post-delete verification.        -- --------------------------------------------------------------------        SET @__reset_remaining = -1;        SET @__reset_sql = CONCAT(            'SELECT COUNT(*) INTO @__reset_remaining FROM `',            REPLACE(v_table, '`', '``'),            '`'        );        PREPARE __reset_stmt FROM @__reset_sql;        EXECUTE __reset_stmt;        DEALLOCATE PREPARE __reset_stmt;        SET v_remaining = COALESCE(@__reset_remaining, -1);        IF v_remaining <> 0 THEN            SET v_message = CONCAT(                'RESET FAILED: table ',                v_table,                ' still contains ',                v_remaining,                ' row(s).'            );            SIGNAL SQLSTATE '45000'                SET MESSAGE_TEXT = v_message;        END IF;        -- --------------------------------------------------------------------        -- Reset AUTO_INCREMENT on empty tables.        -- --------------------------------------------------------------------        SELECT COUNT(*)          INTO v_has_auto_increment        FROM `information_schema`.`COLUMNS`        WHERE `TABLE_SCHEMA` = DATABASE()          AND `TABLE_NAME` = v_table          AND `EXTRA` LIKE '%auto_increment%';        IF v_has_auto_increment > 0 THEN            SET @__reset_sql = CONCAT(                'ALTER TABLE `',                REPLACE(v_table, '`', '``'),                '` AUTO_INCREMENT = 1'            );            PREPARE __reset_stmt FROM @__reset_sql;            EXECUTE __reset_stmt;            DEALLOCATE PREPARE __reset_stmt;        END IF;        -- --------------------------------------------------------------------        -- Record result.        -- --------------------------------------------------------------------        INSERT INTO `__uni_nexus_reset_results`        (            `table_name`,            `rows_deleted`,            `remaining_rows`,            `auto_increment_reset`,            `note`        )        VALUES        (            v_table,            v_deleted,            v_remaining,            IF(v_has_auto_increment > 0, 1, 0),            'CLEARED'        );    END LOOP;    CLOSE reset_cursor;    -- ------------------------------------------------------------------------    -- Restore SQL session safety.    -- ------------------------------------------------------------------------    SET FOREIGN_KEY_CHECKS = @__reset_old_fk_checks;    SET SQL_SAFE_UPDATES   = @__reset_old_safe_updates;END;

#1044 - Access denied for user 'root'@'localhost' to database 'information_schema'