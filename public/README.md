# Public Directory

This directory is used for static browser-level assets that will be served at the root path (`/`).
Intended files to be placed here manually:
- `favicon.ico`
- `favicon.svg`
- `apple-touch-icon.png`
